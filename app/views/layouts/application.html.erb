<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Esercizi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Esercizi">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <%# Load Tailwind CSS %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "quaderno", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "print", media: "print", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <script>
      // Prevent dark mode flash - apply theme before render
      if (localStorage.getItem('darkMode') === 'true' ||
          (!localStorage.getItem('darkMode') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark')
      }
    </script>
  </head>

  <body class="print text-black dark:text-white bg-gray-50 dark:bg-gray-900 h-screen flex flex-col transition-colors duration-300" data-controller="sidebar">
    <%= render "shared/header" %>

    <div class="flex flex-1 overflow-hidden relative">
      <!-- Sidebar -->
      <%= render "shared/sidebar" %>

      <!-- Main Content -->
      <main class="flex-1 overflow-y-auto w-full md:w-auto dark:bg-gray-900">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
