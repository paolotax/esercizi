<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Esercizi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Esercizi">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <%# Load Tailwind CSS %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "quaderno", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "print", media: "print", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <script>
      // Prevent dark mode flash - apply theme before render
      if (localStorage.getItem('darkMode') === 'true' ||
          (!localStorage.getItem('darkMode') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark')
      }
    </script>
  </head>

  <body class="print text-gray-800 dark:text-white bg-bg-playful dark:bg-bg-playful-dark h-screen flex flex-col transition-colors duration-300" data-controller="sidebar">
    <!-- Pattern di sfondo decorativo -->
    <div class="fixed inset-0 pointer-events-none overflow-hidden -z-10">
      <!-- Bolle decorative di sfondo -->
      <div class="absolute top-20 left-10 w-64 h-64 bg-pastel-sky/10 rounded-full blur-3xl"></div>
      <div class="absolute top-40 right-20 w-80 h-80 bg-pastel-pink/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 left-1/4 w-72 h-72 bg-pastel-mint/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-40 right-1/3 w-56 h-56 bg-pastel-peach/10 rounded-full blur-3xl"></div>
      <div class="absolute top-1/2 left-1/2 w-96 h-96 bg-pastel-lavender/5 rounded-full blur-3xl"></div>
      <!-- Pattern dark mode -->
      <div class="hidden dark:block absolute top-10 right-10 w-48 h-48 bg-indigo-500/5 rounded-full blur-2xl"></div>
      <div class="hidden dark:block absolute bottom-10 left-20 w-64 h-64 bg-purple-500/5 rounded-full blur-3xl"></div>
    </div>

    <%= render "shared/header" %>

    <div class="flex flex-1 overflow-hidden relative">
      <!-- Sidebar -->
      <%= render "shared/sidebar" %>

      <!-- Main Content -->
      <main class="flex-1 overflow-y-auto w-full md:w-auto">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
