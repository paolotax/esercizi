<!DOCTYPE html>
<html lang="it" class="h-full">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Esercizi Interattivi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Google Fonts - Nunito per corpo, Fredoka per display %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Nunito:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      :root {
        --font-display: 'Fredoka', sans-serif;
        --font-body: 'Nunito', sans-serif;
        --color-cream: #FFF8F0;
        --color-cream-dark: #F5EBE0;
        --color-coral: #E07A5F;
        --color-coral-dark: #C96A52;
        --color-sage: #81B29A;
        --color-navy: #3D405B;
        --color-gold: #F2CC8F;
      }

      .dark {
        --color-bg: #1a1612;
        --color-bg-elevated: #252019;
        --color-bg-card: #2d261f;
      }

      .font-display { font-family: var(--font-display); }
      .font-body { font-family: var(--font-body); }

      /* Animated gradient background */
      .bg-animated-gradient {
        background: linear-gradient(135deg, var(--color-cream) 0%, var(--color-cream-dark) 50%, #FFE8D6 100%);
        background-size: 200% 200%;
        animation: gradient-shift 15s ease infinite;
      }

      .dark .bg-animated-gradient {
        background: linear-gradient(135deg, #1a1612 0%, #252019 50%, #1f1a15 100%);
        background-size: 200% 200%;
        animation: gradient-shift 15s ease infinite;
      }

      @keyframes gradient-shift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Floating shapes animation */
      .floating-shape {
        position: absolute;
        border-radius: 50%;
        opacity: 0.15;
        animation: float 20s ease-in-out infinite;
      }

      .dark .floating-shape {
        opacity: 0.08;
      }

      @keyframes float {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        33% { transform: translateY(-20px) rotate(120deg); }
        66% { transform: translateY(10px) rotate(240deg); }
      }

      /* Card hover effect */
      .card-lift {
        transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s ease;
      }

      .card-lift:hover {
        transform: translateY(-4px);
      }

      /* Input focus glow */
      .input-glow:focus {
        box-shadow: 0 0 0 3px rgba(224, 122, 95, 0.3);
      }

      .dark .input-glow:focus {
        box-shadow: 0 0 0 3px rgba(224, 122, 95, 0.2);
      }

      /* Button press effect */
      .btn-press {
        transition: transform 0.1s ease, box-shadow 0.1s ease;
      }

      .btn-press:active {
        transform: scale(0.98);
      }

      /* Staggered fade-in animation */
      .fade-in-up {
        animation: fadeInUp 0.6s ease-out forwards;
        opacity: 0;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .delay-100 { animation-delay: 0.1s; }
      .delay-200 { animation-delay: 0.2s; }
      .delay-300 { animation-delay: 0.3s; }
      .delay-400 { animation-delay: 0.4s; }
    </style>
  </head>

  <body class="h-full font-body bg-animated-gradient text-gray-800 dark:text-gray-100 overflow-x-hidden">
    <!-- Floating decorative shapes -->
    <div class="fixed inset-0 pointer-events-none overflow-hidden">
      <div class="floating-shape w-64 h-64 bg-coral -top-20 -left-20" style="animation-delay: 0s; background: var(--color-coral);"></div>
      <div class="floating-shape w-48 h-48 bg-sage top-1/3 -right-16" style="animation-delay: -5s; background: var(--color-sage);"></div>
      <div class="floating-shape w-32 h-32 bg-gold bottom-20 left-1/4" style="animation-delay: -10s; background: var(--color-gold);"></div>
    </div>

    <!-- Main content wrapper -->
    <div class="relative min-h-full flex flex-col">
      <!-- Header -->
      <header class="relative z-10 py-6 px-4">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
          <%= link_to root_path(script_name: nil), class: "flex items-center gap-3 group" do %>
            <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-coral to-coral-dark flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow" style="background: linear-gradient(135deg, var(--color-coral), var(--color-coral-dark));">
              <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
              </svg>
            </div>
            <span class="font-display text-2xl font-bold text-gray-800 dark:text-gray-100">Esercizi</span>
          <% end %>

          <!-- Dark mode toggle -->
          <button type="button"
                  onclick="document.documentElement.classList.toggle('dark')"
                  class="p-3 rounded-xl bg-white/50 dark:bg-white/10 backdrop-blur-sm hover:bg-white/70 dark:hover:bg-white/20 transition-colors">
            <svg class="w-5 h-5 text-gray-600 dark:text-gray-300 dark:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
            </svg>
            <svg class="w-5 h-5 text-gray-300 hidden dark:block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
            </svg>
          </button>
        </div>
      </header>

      <!-- Main content -->
      <main class="relative z-10 flex-1 flex items-center justify-center px-4 py-8">
        <%= yield %>
      </main>

      <!-- Footer -->
      <footer class="relative z-10 py-6 text-center">
        <p class="text-sm text-gray-500 dark:text-gray-400 font-medium">
          &copy; <%= Date.current.year %> Esercizi Interattivi
        </p>
      </footer>
    </div>
  </body>
</html>
