<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Esercizio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Simple header for public view -->
    <div class="bg-white shadow-sm border-b">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="py-4">
          <h1 class="text-xl font-semibold text-gray-900">
            <%= content_for?(:page_title) ? yield(:page_title) : "Esercizio" %>
          </h1>
        </div>
      </div>
    </div>

    <!-- Main content -->
    <main>
      <%= yield %>
    </main>

    <!-- Simple footer -->
    <footer class="mt-auto py-4 text-center text-sm text-gray-500">
      <p>&copy; <%= Date.current.year %> Esercizi</p>
    </footer>
  </body>
</html>