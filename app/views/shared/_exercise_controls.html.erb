<%
  # Parameters:
  # - color: Theme color (default: 'orange')
  # - current_page: Current page identifier (view_template) (optional if @pagina is available)
  # - pagina: Pagina object (optional, will use @pagina if not provided)
  # - show_exercise_buttons: Show Controlla/Soluzioni/Ricomincia buttons (default: true)

  pagina_obj = local_assigns[:pagina] || @pagina
  color = pagina_obj.base_color || local_assigns[:color] || 'orange'
  current_page = local_assigns[:current_page]
  pagina_obj = local_assigns[:pagina] || @pagina
  show_exercise_buttons = local_assigns.fetch(:show_exercise_buttons, true)
  
  # Trova la pagina corrente nel database
  if pagina_obj
    current_pagina = pagina_obj
  elsif current_page
    # Cerca per view_template
    current_pagina = Pagina.find_by(view_template: current_page)
  end
  
  prev_page = nil
  next_page = nil
  
  if current_pagina
    # Trova la pagina precedente e successiva nella stessa disciplina
    # Le pagine sono ordinate per posizione e numero (default_scope)
    all_pages = current_pagina.disciplina.pagine.to_a
    current_index = all_pages.find_index { |p| p.id == current_pagina.id }
    
    if current_index
      prev_page = current_index > 0 ? all_pages[current_index - 1] : nil
      next_page = current_index < all_pages.length - 1 ? all_pages[current_index + 1] : nil
    end
  end
  
  # Color variants for different buttons
  button_colors = {
    'orange' => {
      primary: 'bg-orange-500 hover:bg-orange-600',
      secondary: 'bg-orange-400 hover:bg-orange-500',
      outline: 'border-orange-500 text-orange-600 hover:bg-orange-50 dark:bg-gray-800 dark:border-orange-400 dark:text-orange-400 dark:hover:bg-gray-700'
    },
    'blue' => {
      primary: 'bg-blue-500 hover:bg-blue-600',
      secondary: 'bg-blue-400 hover:bg-blue-500',
      outline: 'border-blue-500 text-blue-600 hover:bg-blue-50 dark:bg-gray-800 dark:border-blue-400 dark:text-blue-400 dark:hover:bg-gray-700'
    },
    'purple' => {
      primary: 'bg-purple-500 hover:bg-purple-600',
      secondary: 'bg-purple-400 hover:bg-purple-500',
      outline: 'border-purple-500 text-purple-600 hover:bg-purple-50 dark:bg-gray-800 dark:border-purple-400 dark:text-purple-400 dark:hover:bg-gray-700'
    },
    'green' => {
      primary: 'bg-green-500 hover:bg-green-600',
      secondary: 'bg-green-400 hover:bg-green-500',
      outline: 'border-green-500 text-green-600 hover:bg-green-50 dark:bg-gray-800 dark:border-green-400 dark:text-green-400 dark:hover:bg-gray-700'
    },
    'pink' => {
      primary: 'bg-pink-500 hover:bg-pink-600',
      secondary: 'bg-pink-400 hover:bg-pink-500',
      outline: 'border-pink-500 text-pink-600 hover:bg-pink-50 dark:bg-gray-800 dark:border-pink-400 dark:text-pink-400 dark:hover:bg-gray-700'
    },
    'red' => {
      primary: 'bg-red-500 hover:bg-red-600',
      secondary: 'bg-red-400 hover:bg-red-500',
      outline: 'border-red-500 text-red-600 hover:bg-red-50 dark:bg-gray-800 dark:border-red-400 dark:text-red-400 dark:hover:bg-gray-700'
    },
    'yellow' => {
      primary: 'bg-yellow-500 hover:bg-yellow-600',
      secondary: 'bg-yellow-400 hover:bg-yellow-500',
      outline: 'border-yellow-500 text-yellow-600 hover:bg-yellow-50 dark:bg-gray-800 dark:border-yellow-400 dark:text-yellow-400 dark:hover:bg-gray-700'
    },
    'cyan' => {
      primary: 'bg-cyan-500 hover:bg-cyan-600',
      secondary: 'bg-cyan-400 hover:bg-cyan-500',
      outline: 'border-cyan-500 text-cyan-600 hover:bg-cyan-50 dark:bg-gray-800 dark:border-cyan-400 dark:text-cyan-400 dark:hover:bg-gray-700'
    }
  }
  
  colors = button_colors[color] || button_colors['orange']
%>

<div class="print-hidden mt-8 mb-4" data-exercise-checker-target="buttonContainer">
  <!-- All buttons on same line -->
  <div class="flex flex-wrap gap-4 justify-between items-center">
    <!-- Previous Page Button -->
    <div class="flex-shrink-0">
      <% if prev_page %>
        <%= link_to pagina_path(prev_page.slug),
            data: { turbo_frame: "_top" },
            class: "flex items-center justify-center #{colors[:primary]} text-white rounded-full shadow-lg transition transform hover:scale-105 w-12 h-12 sm:w-14 sm:h-14" do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        <% end %>
      <% else %>
        <div class="flex items-center justify-center bg-gray-300 dark:bg-gray-700 text-gray-500 dark:text-gray-400 rounded-full cursor-not-allowed opacity-50 w-12 h-12 sm:w-14 sm:h-14">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </div>
      <% end %>
    </div>

    <% if show_exercise_buttons %>
      <!-- Center control buttons -->
      <div class="flex flex-wrap gap-3 md:gap-4 justify-center items-center flex-1">

        <!-- Reset Button -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-800 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                onclick="window.location.href = window.location.href.split('?')[0] + '?t=' + Date.now()"
                title="Ricomincia">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
        </button>

        <!-- Check Answers Button -->
        <button class="<%= colors[:primary] %> text-white rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->exercise-checker#checkAnswers"
                title="Controlla">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </button>

        <!-- Solve Button -->
        <button class="<%= colors[:secondary] %> text-white rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->exercise-checker#showSolutions"
                title="Soluzioni">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
          </svg>
        </button>

        <!-- Text Toggle Button (MAIUSCOLO/minuscolo) -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-800 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->text-toggle#toggle"
                data-text-toggle-target="button"
                title="Toggle maiuscolo/minuscolo">
          <span class="text-lg sm:text-xl font-bold" data-text-toggle-target="buttonText">AA</span>
        </button>

        <!-- Font Toggle Button (mono/sans) -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-800 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->font-controls#toggleFont"
                title="Toggle font mono/sans">
          <span class="text-lg sm:text-xl font-bold" data-font-controls-target="fontButton">F</span>
        </button>

        <!-- Decrease Font Size Button -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-800 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->font-controls#decrease"
                title="Diminuisci testo">
          <span class="text-lg sm:text-xl font-bold">A-</span>
        </button>

        <!-- Increase Font Size Button -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-800 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-action="click->font-controls#increase"
                title="Ingrandisci testo">
          <span class="text-lg sm:text-xl font-bold">A+</span>
        </button>

        <!-- Dark Mode Toggle Button -->
        <button class="border-2 <%= colors[:outline] %> bg-white dark:bg-gray-700 dark:border-gray-500 dark:text-gray-200 rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14"
                data-controller="dark-mode"
                data-action="click->dark-mode#toggle"
                title="Toggle dark mode">
          <!-- Sun icon (shown in dark mode) -->
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7 hidden dark:block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
          </svg>
          <!-- Moon icon (shown in light mode) -->
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7 block dark:hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
          </svg>
        </button>

        <!-- Text to Speech Button (Leggi) - hidden by default -->
        <button class="bg-blue-500 hover:bg-blue-600 text-white rounded-full shadow-lg transition transform hover:scale-105 flex items-center justify-center w-12 h-12 sm:w-14 sm:h-14 hidden"
                data-controller-check="text-to-speech"
                data-action="click->text-to-speech#read"
                data-text-to-speech-target="button"
                title="Leggi il testo ad alta voce">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z" />
          </svg>
        </button>
      </div>
    <% end %>
    
    <!-- Next Page Button -->
    <div class="flex-shrink-0">
      <% if next_page %>
        <%= link_to pagina_path(next_page.slug),
            data: { turbo_frame: "_top" },
            class: "flex items-center justify-center #{colors[:primary]} text-white rounded-full shadow-lg transition transform hover:scale-105 w-12 h-12 sm:w-14 sm:h-14" do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        <% end %>
      <% else %>
        <div class="flex items-center justify-center bg-gray-300 dark:bg-gray-700 text-gray-500 dark:text-gray-400 rounded-full cursor-not-allowed opacity-50 w-12 h-12 sm:w-14 sm:h-14">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 sm:w-7 sm:h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </div>
      <% end %>
    </div>
  </div>
</div>


