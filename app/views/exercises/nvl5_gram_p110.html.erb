<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Info Box -->
  <div class="bg-<%= @pagina.base_color %>-50 p-4 md:p-6 pb-8 rounded-lg">
    <div class="flex flex-col lg:flex-row gap-6 items-start">
      <div class="flex-1">
        <ul class="space-y-3 text-base md:text-lg text-gray-700">
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <span>I <strong>complementi di luogo</strong> sono complementi indiretti;</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <span>indicano il luogo in cui avviene l'azione espressa dal predicato;</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <div>
              <span>rispondono alle domande: <strong>dove?</strong> / <strong>da dove?</strong> / <strong>per dove?</strong> / <strong>attraverso dove?</strong> / <strong>verso dove?</strong></span>
              <div class="mt-2 space-y-1 text-sm italic">
                <div>→ Veniamo (<em>da dove?</em>) <strong class="text-purple-600">dalla città</strong>.</div>
                <div>→ Stiamo andando (<em>dove?</em>) <strong class="text-blue-600">nel bosco</strong>.</div>
              </div>
            </div>
          </li>
        </ul>
      </div>
      <div class="flex-shrink-0">
        <%= image_tag "nvl5_gram/p110/p110_01.jpg", class: "w-full lg:w-64 rounded-lg", alt: "Bambini che camminano" %>
      </div>
    </div>
  </div>

  <!-- Exercise 1: IMPARARE TUTTI -->
  <div class="bg-<%= @pagina.base_color %>-100 p-4 md:p-6 pb-8 rounded-lg" data-controller="word-highlighter">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      <span class="bg-yellow-400 text-blue-900 px-3 py-1 rounded-lg font-bold">IMPARARE TUTTI</span>
      Dividi la frase in sintagmi, sottolinea il complemento di luogo e scrivi a quale domanda risponde.
    </h2>

    <div class="space-y-3 text-base md:text-lg">
      <!-- Esempio -->
      <div class="bg-white rounded-lg p-4 border-2 border-green-400">
        <div class="flex items-start gap-2">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <div class="flex-1">
            <div class="mb-2">
              Molte persone <strong>/</strong>
              <span class="bg-blue-200 px-1 rounded">in America</span>
              <strong>/</strong> amano <strong>/</strong> la pizza.
            </div>
            <div class="text-green-600 font-bold">Dove?</div>
          </div>
        </div>
      </div>

      <% frases = [
        { text: "Vado da Luca in auto con Enrico.", complement: "da Luca", answer: "dove?" },
        { text: "Io e Sandro arriviamo da scuola alle tre.", complement: "da scuola", answer: "da dove?" },
        { text: "Noi oggi passiamo per il quartiere.", complement: "per il quartiere", answer: "per dove?" },
        { text: "Lavoro in un palazzo all'Eur, a Roma.", complement: ["in un palazzo", "all'Eur", "a Roma"], answer: "dove?" },
        { text: "Andrò a piedi da Bologna a Firenze.", complement: ["da Bologna", "a Firenze"], answer: ["da dove?", "verso dove?"] }
      ] %>

      <% frases.each_with_index do |frase, index| %>
        <div class="bg-white rounded-lg p-4" data-controller="fill-blanks">
          <div class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <div class="flex-1">
              <div class="mb-2">
                <% parts = frase[:text].split(/\s+/) %>
                <% parts.each_with_index do |word, i| %>
                  <% if [frase[:complement]].flatten.any? { |c| frase[:text].include?(c) && c.include?(word) } %>
                    <span class="transition cursor-pointer hover:bg-blue-100 px-1 rounded"
                          data-word-highlighter-target="word"
                          data-correct="true"
                          data-action="click->word-highlighter#toggleHighlight"><%= word %></span>
                  <% else %>
                    <%= word %>
                  <% end %>
                  <%= " " unless i == parts.length - 1 %>
                <% end %>
              </div>
              <input type="text"
                     class="w-full md:w-64 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 focus:outline-none"
                     data-fill-blanks-target="input"
                     data-correct-answer="<%= [frase[:answer]].flatten.first %>">
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2 -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Completa le frasi con un complemento di luogo adatto.
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-3 text-base md:text-lg">
      <% [
        "Giacomo va",
        "La nave è diretta",
        "Andiamo spesso",
        "I viaggiatori giunsero",
        "Mia madre lavora",
        "Zia Irene sta"
      ].each do |inizio| %>
        <div class="bg-<%= @pagina.base_color %>-50 rounded-lg p-4">
          <div class="flex flex-wrap items-center gap-2">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="text-gray-700"><%= inizio %></span>
            <input type="text"
                   class="flex-1 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 focus:outline-none"
                   data-fill-blanks-target="input"
                   data-correct-answer="libera">
            <span class="text-gray-700">.</span>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 3 -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      Scrivi sul quaderno una frase per ogni coppia di parole usando il verbo come predicato e il sostantivo in un complemento di luogo.
    </h2>

    <div class="flex flex-wrap gap-4 justify-center text-base md:text-lg">
      <% [
        ["passeggiare", "parco"],
        ["raggiungere", "stazione"],
        ["vivere", "Italia"]
      ].each do |verbo, sostantivo| %>
        <div class="bg-<%= @pagina.base_color %>-50 rounded-lg p-4 flex gap-3">
          <div class="bg-white px-4 py-2 rounded font-semibold text-<%= @pagina.base_color %>-700">
            <%= verbo %>
          </div>
          <div class="bg-white px-4 py-2 rounded font-semibold text-<%= @pagina.base_color %>-700">
            <%= sostantivo %>
          </div>
        </div>
      <% end %>
    </div>

    <div class="mt-4 p-4 bg-yellow-50 rounded-lg">
      <p class="text-gray-600 italic text-sm">
        Suggerimento: Ricorda di usare le preposizioni corrette (in, a, da, per, verso, attraverso)
        per formare il complemento di luogo.
      </p>
    </div>
  </div>

  <!-- Reference -->
  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "185", pagina_path("nvl5_gram_p185"), class: "text-gray-600 hover:text-gray-800 underline" %> e <%= link_to "187", pagina_path("nvl5_gram_p187"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <%= render 'shared/exercise_controls' %>

</div>
