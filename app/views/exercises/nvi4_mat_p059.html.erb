<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls fraction-coloring"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Sezione 1: Cerchio diviso in 6 parti -->
    <div class="py-4">
      <p class="text-gray-700 dark:text-gray-200 mb-4">
        <span class="text-<%= @pagina.base_color %>-500">•</span> Osserva: il <span class="font-bold">cerchio</span> è stato diviso in 6 parti uguali.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
        <!-- Cerchio -->
        <div class="flex justify-center">
          <div class="relative w-40 h-40">
            <svg viewBox="0 0 100 100" class="w-full h-full">
              <% 6.times do |i| %>
                <path d="M50,50 L<%= 50 + 45 * Math.cos((i * 60 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin((i * 60 - 90) * Math::PI / 180) %> A45,45 0 0,1 <%= 50 + 45 * Math.cos(((i + 1) * 60 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin(((i + 1) * 60 - 90) * Math::PI / 180) %> Z"
                      fill="<%= i < 4 ? '#fdba74' : 'white' %>"
                      stroke="#9ca3af"
                      stroke-width="1"
                      class="cursor-pointer hover:opacity-80"
                      data-action="click->fraction-coloring#toggleColor"/>
              <% end %>
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9ca3af" stroke-width="2"/>
            </svg>
          </div>
        </div>

        <!-- Spiegazione -->
        <div class="space-y-3">
          <p class="text-gray-700 dark:text-gray-200">
            Considera quattro volte l'unità frazionaria e colorala come preferisci: hai colorato <%= render 'shared/frazione', num: 4, den: 6 %> del cerchio.
          </p>
          <div class="flex items-center gap-2 flex-wrap">
            <%= render 'shared/frazione', num: 1, den: 6 %>
            <span class="text-gray-700 dark:text-gray-200">+</span>
            <%= render 'shared/frazione', num: 1, den: 6 %>
            <span class="text-gray-700 dark:text-gray-200">+</span>
            <%= render 'shared/frazione', num: 1, den: 6 %>
            <span class="text-gray-700 dark:text-gray-200">+</span>
            <%= render 'shared/frazione', num: 1, den: 6 %>
            <span class="text-gray-700 dark:text-gray-200">=</span>
            <%= render 'shared/frazione', num: 4, den: 6 %>
          </div>
        </div>
      </div>
    </div>

    <!-- Box regola -->
    <div class="p-3 bg-pink-light dark:bg-pink-900/40 rounded-2xl">
      <p class="text-gray-700 dark:text-gray-200">
        La <span class="font-bold">frazione</span> è un <span class="font-bold">operatore</span> che ti permette di dividere l'intero in parti uguali (quante ne indica il denominatore) e di considerarne alcune (quante ne indica il numeratore).
      </p>
    </div>

    <!-- Sezione 2: Segmento -->
    <div class="py-4 border-t border-gray-200 dark:border-gray-700">
      <p class="text-gray-700 dark:text-gray-200 mb-4">
        <span class="text-<%= @pagina.base_color %>-500">•</span> Considera come intero questo <span class="font-bold">segmento</span>. Colorane <%= render 'shared/frazione', num: 3, den: 5 %>.
      </p>

      <div class="mb-4">
        <div class="w-full max-w-lg h-10 border-2 border-gray-400 grid grid-cols-5">
          <% 5.times do |i| %>
            <div class="<%= i < 4 ? 'border-r border-gray-400' : '' %> cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
          <% end %>
        </div>
      </div>

      <div class="space-y-2">
        <p class="text-gray-700 dark:text-gray-200">
          Dividi il segmento in
          <input type="text" data-correct-answer="5" class="w-12 border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
          parti uguali.
        </p>
        <p class="text-gray-700 dark:text-gray-200">
          Ogni parte (<%= render 'shared/frazione', num: 1, den: 5 %>) è un'unità frazionaria.
        </p>
        <p class="text-gray-700 dark:text-gray-200">
          Considera
          <input type="text" data-correct-answer="3" class="w-12 border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
          volte l'unità frazionaria e colorala come preferisci: hai colorato <%= render 'shared/frazione', num: 3, den: 5 %> del segmento.
        </p>
      </div>

      <div class="flex items-center gap-2 mt-4 flex-wrap">
        <%= render 'shared/frazione', num: 1, den: 5 %>
        <span class="text-gray-700 dark:text-gray-200">+</span>
        <%= render 'shared/frazione', num: 1, den: 5 %>
        <span class="text-gray-700 dark:text-gray-200">+</span>
        <%= render 'shared/frazione', num: 1, den: 5 %>
        <span class="text-gray-700 dark:text-gray-200">=</span>
        <%= render 'shared/frazione', num: 3, den: 5 %>
      </div>
    </div>

    <!-- Sezione 3: Caramelle -->
    <div class="py-4 border-t border-gray-200 dark:border-gray-700">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <p class="text-gray-700 dark:text-gray-200 mb-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span> Considera come intero le 24 <span class="font-bold">caramelle</span> nel sacchetto. Colorane <%= render 'shared/frazione', num: 2, den: 6 %>.<br>
            Scrivi ciò che devi fare.
          </p>

          <div class="space-y-3">
            <div class="flex items-center gap-2 flex-wrap">
              <span class="text-gray-700 dark:text-gray-200">Divido l'intera quantità</span>
              <input type="text" data-correct-answer="24" class="w-14 border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
              <span class="text-gray-700 dark:text-gray-200">in</span>
              <input type="text" data-correct-answer="6 parti uguali" class="min-w-0 flex-1 max-w-xs border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
            </div>
            <div class="flex items-center gap-2 flex-wrap">
              <span class="text-gray-700 dark:text-gray-200">Ogni</span>
              <input type="text" data-correct-answer="parte è 4 caramelle" class="min-w-0 flex-1 max-w-xs border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
            </div>
            <div class="flex items-center gap-2 flex-wrap">
              <span class="text-gray-700 dark:text-gray-200">Considero</span>
              <input type="text" data-correct-answer="2 parti" class="min-w-0 flex-1 max-w-xs border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
            </div>
            <div class="flex items-center gap-2 flex-wrap">
              <input type="text" data-correct-answer="2 × 4 = 8" class="w-24 border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
              <span class="text-gray-700 dark:text-gray-200">: ho colorato</span>
              <input type="text" data-correct-answer="8 caramelle" class="min-w-0 flex-1 max-w-xs border-b-2 border-dotted border-gray-400 dark:border-gray-500 text-center font-bold bg-transparent dark:text-white">
            </div>
          </div>

          <div class="flex items-center gap-2 mt-4 flex-wrap">
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "1", den_answer: "6" %>
            <span class="text-gray-700 dark:text-gray-200">+</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "1", den_answer: "6" %>
            <span class="text-gray-700 dark:text-gray-200">=</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "2", den_answer: "6" %>
          </div>
        </div>

        <!-- Immagine caramelle -->
        <div class="flex justify-center">
          <%= image_tag "nvi4_mat/p059/p059_08.jpg", class: "max-w-full h-auto dark:bg-white dark:rounded-lg dark:p-2" %>
        </div>
      </div>
    </div>

    <%= render 'shared/quaderno_link', pagine: [201, 204, 206, 207] %>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
