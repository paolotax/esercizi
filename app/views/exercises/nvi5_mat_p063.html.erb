<div class="max-w-6xl mx-auto bg-white p-3 md:p-6"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Problema iniziale con immagine -->
    <div class="flex flex-col md:flex-row gap-6">
      <div class="flex-1">
        <div class="p-4 bg-cyan-50 border-3 border-cyan-400 rounded-2xl mb-4">
          <p class="text-gray-800 mb-2">
            Alessia ha letto <%= render 'shared/frazione', num: 3, den: 8, bold: true %> delle 192 pagine del suo libro di astronomia.
          </p>
          <p class="text-gray-700">
            Quante pagine deve ancora leggere per finire il libro?
          </p>
        </div>

        <p class="text-gray-700 mb-4">Per rispondere alla domanda puoi procedere in modi diversi.</p>
      </div>
      <div class="flex-shrink-0">
        <%= image_tag "nvi5_mat/p063/p063_05.jpg", alt: "Alessia che legge", class: "max-w-xs rounded-lg" %>
      </div>
    </div>

    <!-- Primo modo -->
    <div>
      <div class="inline-block bg-orange-400 text-white font-bold px-3 py-1 rounded mb-3">Primo modo</div>
      <p class="text-gray-700 mb-4">Calcola il numero di pagine che Alessia ha già letto, poi calcola quante pagine le restano da leggere.</p>

      <div class="space-y-2 mb-4 ml-4">
        <p class="text-gray-800 text-lg">
          <%= render 'shared/frazione', num: 3, den: 8, bold: true %> di 192
        </p>
        <p class="text-gray-800">
          (192 : 8) × 3 = 24 × 3 = 72 <span class="text-pink-600 font-bold">← numero pagine già lette</span>
        </p>
        <p class="text-gray-800">
          192 − 72 = 120 <span class="text-pink-600 font-bold">← numero pagine da leggere</span>
        </p>
      </div>

      <div class="p-4 bg-cyan-100 inline-block">
        <p class="text-gray-700">
          Per calcolare la frazione di un numero <strong>dividi</strong> quel numero <strong>per
          il denominatore</strong> e <strong>moltiplica</strong> il
          risultato <strong>per il numeratore</strong>.
        </p>
      </div>
    </div>

    <!-- Secondo modo -->
    <div>
      <div class="inline-block bg-orange-400 text-white font-bold px-3 py-1 rounded mb-3">Secondo modo</div>
      <p class="text-gray-700 mb-4">
        Trova la <strong>frazione complementare</strong>, che corrisponde al numero delle pagine che Alessia
        deve ancora leggere, e calcolane il valore.
      </p>

      <div class="space-y-2 mb-4 ml-4">
        <p class="text-gray-800 text-lg">
          <%= render 'shared/frazione', num: 8, den: 8, bold: true %> −
          <%= render 'shared/frazione', num: 3, den: 8, bold: true %> =
          <%= render 'shared/frazione', num: 5, den: 8, bold: true %>
          <span class="text-pink-600 font-bold ml-2">← frazione corrispondente alle pagine da leggere</span>
        </p>
        <p class="text-gray-800 text-lg">
          <%= render 'shared/frazione', num: 5, den: 8, bold: true %> di 192
        </p>
        <p class="text-gray-800">
          (192 : 8) × 5 = 24 × 5 = 120 <span class="text-pink-600 font-bold">← numero pagine da leggere</span>
        </p>
      </div>

      <p class="text-gray-700 mb-3">Naturalmente arrivi alla stessa risposta:</p>
      <div class="p-3 bg-cyan-100 inline-block">
        <p class="text-gray-800">
          Deve ancora leggere
          <input type="text" data-correct-answer="120" class="w-16 px-2 py-1 border-2 border-pink-400 rounded text-center font-bold bg-white mx-1">
          pagine per finire il libro.
        </p>
      </div>
    </div>

    <!-- ESERCIZI -->
    <div class="p-4 md:p-6 bg-orange-100 rounded-lg">
      <h2 class="font-bold text-<%= @pagina.base_color %>-600 mb-4 text-xl">ESERCIZI</h2>

      <div class="grid grid-cols-1 md:grid-cols-2 divide-y-2 md:divide-y-0 md:divide-x-2 divide-<%= @pagina.base_color %>-300">

        <!-- Colonna sinistra: Esercizio 1 -->
        <div class="p-4 rounded-lg">
          <div class="flex items-start gap-3 mb-4">
            <%= numero_esercizio_badge(1, colore: "blue-500") %>
            <p class="font-bold text-gray-700">Calcola.</p>
          </div>

          <div class="space-y-4">
            <!-- 3/5 di 250 -->
            <div class="flex items-center gap-3 flex-wrap">
              <%= render 'shared/frazione', num: 3, den: 5, bold: true %>
              <span class="text-gray-700">di 250</span>
              <span class="text-pink-600 font-bold">→</span>
              <input type="text" data-correct-answer="150" class="w-20 px-2 py-1 border-2 border-pink-400 rounded text-center font-bold bg-white">
            </div>

            <!-- 2/7 di 49 -->
            <div class="flex items-center gap-3 flex-wrap">
              <%= render 'shared/frazione', num: 2, den: 7, bold: true %>
              <span class="text-gray-700">di 49</span>
              <span class="text-pink-600 font-bold">→</span>
              <input type="text" data-correct-answer="14" class="w-20 px-2 py-1 border-2 border-pink-400 rounded text-center font-bold bg-white">
            </div>

            <!-- 4/5 di 450 -->
            <div class="flex items-center gap-3 flex-wrap">
              <%= render 'shared/frazione', num: 4, den: 5, bold: true %>
              <span class="text-gray-700">di 450</span>
              <span class="text-pink-600 font-bold">→</span>
              <input type="text" data-correct-answer="360" class="w-20 px-2 py-1 border-2 border-pink-400 rounded text-center font-bold bg-white">
            </div>

            <!-- 8/12 di 2400 -->
            <div class="flex items-center gap-3 flex-wrap">
              <%= render 'shared/frazione', num: 8, den: 12, bold: true %>
              <span class="text-gray-700">di 2400</span>
              <span class="text-pink-600 font-bold">→</span>
              <input type="text" data-correct-answer="1600" class="w-20 px-2 py-1 border-2 border-pink-400 rounded text-center font-bold bg-white">
            </div>
          </div>
        </div>

        <!-- Colonna destra: Esercizi 2 e 3 -->
        <div class="p-4 md:pl-6 space-y-6">
          <!-- Esercizio 2 -->
          <div>
            <div class="flex items-start gap-3">
              <%= numero_esercizio_badge(2, colore: "blue-500") %>
              <p class="text-gray-700">
                Il papà di Matteo ha comprato uno scooter
                che costa 1 200 euro e ha versato subito un acconto
                pari a <%= render 'shared/frazione', num: 2, den: 6, bold: true %> del prezzo totale. Quanto deve pagare
                ancora?
              </p>
            </div>
          </div>

          <!-- Esercizio 3 -->
          <div>
            <div class="flex items-start gap-3">
              <%= numero_esercizio_badge(3, colore: "blue-500") %>
              <p class="text-gray-700">
                Da un nastro di seta lungo 18 m sono stati
                tagliati prima <%= render 'shared/frazione', num: 2, den: 9, bold: true %>, poi <%= render 'shared/frazione', num: 3, den: 9, bold: true %>. Quanti metri sono
                rimasti?
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%= render 'shared/exercise_controls', color: 'blue' %>
  </div>
</div>
