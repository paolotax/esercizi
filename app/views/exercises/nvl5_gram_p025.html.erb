<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 text-black dark:text-white p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

  <!-- Introduction -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8">
    <p class="text-gray-700 dark:text-gray-300">
      <span class="text-<%= @pagina.base_color %>-500">•</span> In Italia ci sono molti <strong>dialetti</strong>, cioè lingue locali. Oggi sono meno usate che in passato,
      anche se sono ancora parlate in famiglia e tra amici.
    </p>
  </div>

  <!-- Curiosity Box -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8">
    <h2 class="font-bold text-gray-800 dark:text-gray-200 mb-3 flex items-center gap-2">
      <span class="">💡</span>
      Curiosità
    </h2>
    <p class="text-gray-700 dark:text-gray-300 mb-2">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Dal Veneto viene la parola italiana più conosciuta al mondo: <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">CIAO!</strong>
    </p>
    <p class="text-gray-700 dark:text-gray-300 mb-2">
      <span class="text-<%= @pagina.base_color %>-500">•</span> La parola da cui deriva è <em>s'ciavo</em> (schiavo).
    </p>
    <p class="text-gray-700 dark:text-gray-300">
      <span class="text-<%= @pagina.base_color %>-500">•</span> <em>S'ciavo</em> era un saluto rispettoso usato a Venezia: significava, più o meno, "servo suo".
    </p>
  </div>

  <!-- Exercise 1 -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="font-bold text-gray-800 dark:text-gray-200 mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Scrivi il significato di ogni parola e la regione italiana dalla quale proviene.
    </h2>
    <p class="text-gray-600 dark:text-gray-400 mb-4">
      Potete lavorare dividendovi in classe in piccoli gruppi e usando il dizionario.
    </p>

    <% dialect_words = {
      "ciofeca" => "bevanda di cattiva qualità - Lombardia",
      "fasullo" => "falso, contraffatto - Veneto",
      "grissino" => "pane lungo e sottile - Piemonte",
      "cassata" => "dolce tipico siciliano - Sicilia"
    } %>

    <div class="space-y-4 text-gray-800 dark:text-gray-200">
      <% dialect_words.each do |word, answer| %>
        <div class="rounded-lg p-3">
          <div class="font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= word %></div>
          <input type="text"
                 class="w-full border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white py-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= answer %>"
                 data-correct-answer="<%= answer %>">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
  </div><!-- chiude space-y-6 -->
</div>
