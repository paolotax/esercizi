<div class="max-w-6xl mx-auto p-3 md:p-8 bg-gradient-to-br from-red-50 via-white to-orange-50 rounded-3xl shadow-xl" data-controller="exercise-checker">

  <!-- Header -->
  <div class="mb-8">
    <div class="flex items-start sm:items-center justify-between gap-4 mb-4">
      <div class="flex flex-col-reverse sm:flex-row items-start sm:items-center gap-2 sm:gap-4">
        <div class="bg-red-500 text-white text-md sm:text-xl px-2 sm:px-4 py-1.5 sm:py-2 rounded-lg font-bold">
          VERIFICA
        </div>
        <h1 class="text-2xl sm:text-3xl font-bold text-red-600">Il lessico (Parte 1)</h1>
      </div>
      <div class="flex-shrink-0 bg-red-600 text-white rounded-full w-10 h-10 sm:w-12 sm:h-12 flex items-center justify-center font-bold text-lg sm:text-xl">
        38
      </div>
    </div>
  </div>

  <!-- Exercise 1: Complete with Latin words -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-blue-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-3">
      <span class="font-bold">1</span> Completa le frasi con la parola latina adatta. Scegli fra: horror • gratis • habitat • bis • retro • album.
    </h2>

    <% latin_exercises = [
      { text: "Prendi un campione di questa crema: è", answer: "gratis" },
      { text: "Il garage è sul", answer: "retro", suffix: "della casa." },
      { text: "Il mio", answer: "album", suffix: "delle figurine è completo." },
      { text: "Ottima la pasta! Faccio il", answer: "bis" },
      { text: "Questo è un film", answer: "horror", suffix: ", quindi non è adatto a me." },
      { text: "Ogni animale vive bene nel proprio", answer: "habitat" }
    ] %>

    <div class="space-y-3 text-base md:text-lg">
      <% latin_exercises.each_with_index do |exercise, index| %>
        <div class="bg-blue-50 border-2 border-blue-200 rounded-xl px-3 py-3 flex flex-wrap items-center gap-2">
          <span class="text-gray-700"><%= exercise[:text] %></span>
          <input type="text"
                 class="border-b-2 border-blue-500 bg-transparent px-2 py-1 text-center focus:outline-none focus:border-blue-700 w-32"
                 data-fill-blanks-target="input"
                 data-answer="<%= exercise[:answer] %>"
                 data-correct-answer="<%= exercise[:answer] %>"
                 placeholder="..................."
                 maxlength="20">
          <% if exercise[:suffix] %>
            <span class="text-gray-700"><%= exercise[:suffix] %></span>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Match foreign words with Italian -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-purple-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-purple-700 mb-3">
      <span class="font-bold">2</span> Colora allo stesso modo la parola di origine straniera e la corrispondente parola italiana.
    </h2>

    <div class="bg-purple-50 border-2 border-purple-200 rounded-xl px-4 py-4 mb-3">
      <p class="text-base md:text-lg text-purple-700 font-semibold mb-2">Parole straniere:</p>
      <p class="text-base md:text-lg text-gray-700">dessert | balloon | fashion | break | chef | t-shirt</p>
    </div>

    <div class="bg-purple-50 border-2 border-purple-200 rounded-xl px-4 py-4">
      <p class="text-base md:text-lg text-indigo-700 font-semibold mb-2">Parole italiane:</p>
      <p class="text-base md:text-lg text-gray-700">capocuoco | pausa | nuvoletta | dolce | maglietta | alla moda</p>
    </div>
  </div>

  <!-- Exercise 3: Identify non-synonyms -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-green-200 shadow-2xl p-4 md:p-6" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold text-green-700 mb-3">
      <span class="font-bold">3</span> In ogni gruppo indica con una X la parola che NON è sinonimo di quella evidenziata.
    </h2>

    <% synonym_groups = [
      { word: "dubbio", options: ["incertezza", "notizia", "perplessità"], non_synonym: "notizia" },
      { word: "facile", options: ["chiaro", "semplice", "conosciuto"], non_synonym: "conosciuto" },
      { word: "capire", options: ["scegliere", "comprendere", "intuire"], non_synonym: "scegliere" },
      { word: "evidente", options: ["ovvio", "paziente", "lampante"], non_synonym: "paziente" }
    ] %>

    <div class="space-y-3 text-base md:text-lg">
      <% synonym_groups.each_with_index do |group, group_index| %>
        <div class="bg-green-50 border-2 border-green-200 rounded-xl px-3 py-3">
          <p class="text-gray-700 font-semibold mb-2"><%= group[:word] %> →</p>
          <div class="flex flex-wrap gap-4 ml-4">
            <% group[:options].each do |option| %>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox"
                       name="synonym_group<%= group_index %>"
                       value="<%= option %>"
                       data-correct-answer="<%= group[:non_synonym] == option ? 'true' : 'false' %>"
                       class="w-5 h-5">
                <span><%= option %></span>
              </label>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 4: Identify antonyms -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-amber-200 shadow-2xl p-4 md:p-6" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold text-amber-700 mb-3">
      <span class="font-bold">4</span> In ogni gruppo indica con una X il contrario della parola evidenziata.
    </h2>

    <% antonym_groups = [
      { word: "secco", options: ["asciutto", "arido", "umido"], antonym: "umido" },
      { word: "tirare", options: ["trascinare", "spingere", "tendere"], antonym: "spingere" },
      { word: "raro", options: ["eccezionale", "straordinario", "frequente"], antonym: "frequente" },
      { word: "apposta", options: ["volutamente", "involontariamente", "intenzionalmente"], antonym: "involontariamente" }
    ] %>

    <div class="space-y-3 text-base md:text-lg">
      <% antonym_groups.each_with_index do |group, group_index| %>
        <div class="bg-amber-50 border-2 border-amber-200 rounded-xl px-3 py-3">
          <p class="text-gray-700 font-semibold mb-2"><%= group[:word] %> →</p>
          <div class="flex flex-wrap gap-4 ml-4">
            <% group[:options].each do |option| %>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox"
                       name="antonym_group<%= group_index %>"
                       value="<%= option %>"
                       data-correct-answer="<%= group[:antonym] == option ? 'true' : 'false' %>"
                       class="w-5 h-5">
                <span><%= option %></span>
              </label>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 5: Polysemic word "pianta" -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-teal-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-teal-700 mb-3">
      <span class="font-bold">5</span> La parola "pianta" è polisemica. Scrivi due frasi usandola con significati diversi.
    </h2>

    <div class="space-y-3 text-base md:text-lg">
      <div class="bg-teal-50 border-2 border-teal-200 rounded-xl px-3 py-3">
        <span class="text-gray-700">1. .................................................................</span>
      </div>
      <div class="bg-teal-50 border-2 border-teal-200 rounded-xl px-3 py-3">
        <span class="text-gray-700">2. .................................................................</span>
      </div>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. 144
  </div>

  <!-- Exercise Controls -->
  <div class="mt-6">
    <%= render 'shared/exercise_controls', color: 'red', current_page: 'nvl5_gram_p038' %>
  </div>
</div>
