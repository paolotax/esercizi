<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 text-black dark:text-white p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Exercise 1: Complete with Latin words -->
  <div class="bg-white dark:bg-gray-800 p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Completa le frasi con la parola latina adatta. Scegli fra: horror - gratis - habitat - bis - retro - album.
    </h2>

    <% latin_exercises = [
      { text: "Prendi un campione di questa crema: è", answer: "gratis" },
      { text: "Il garage è sul", answer: "retro", suffix: "della casa." },
      { text: "Il mio", answer: "album", suffix: "delle figurine è completo." },
      { text: "Ottima la pasta! Faccio il", answer: "bis" },
      { text: "Questo è un film", answer: "horror", suffix: ", quindi non è adatto a me." },
      { text: "Ogni animale vive bene nel proprio", answer: "habitat" }
    ] %>

    <div class="space-y-3">
      <% latin_exercises.each_with_index do |exercise, index| %>
        <div class="rounded-lg p-3 flex flex-wrap items-center gap-2">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <span class="text-gray-700 dark:text-gray-200"><%= exercise[:text] %></span>
          <input type="text"
                 class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= exercise[:answer] %>"
                 data-correct-answer="<%= exercise[:answer] %>"
                 maxlength="20">
          <% if exercise[:suffix] %>
            <span class="text-gray-700 dark:text-gray-200"><%= exercise[:suffix] %></span>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Match foreign words with Italian -->
  <div class="bg-white dark:bg-gray-800 p-4 md:p-6 pb-8">
    <h2 class="font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Colora allo stesso modo la parola di origine straniera e la corrispondente parola italiana.
    </h2>

    <div class="bg-<%= @pagina.base_color %>-50 dark:bg-<%= @pagina.base_color %>-900/30 rounded-lg p-3 mb-3">
      <p class="text-<%= @pagina.base_color %>-700 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> Parole straniere:</p>
      <p class="text-gray-700 dark:text-gray-200">dessert | balloon | fashion | break | chef | t-shirt</p>
    </div>

    <div class="bg-gray-50 dark:bg-gray-700 rounded-lg p-3">
      <p class="text-gray-700 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> Parole italiane:</p>
      <p class="text-gray-700 dark:text-gray-200">capocuoco | pausa | nuvoletta | dolce | maglietta | alla moda</p>
    </div>
  </div>

  <!-- Exercise 3: Identify non-synonyms -->
  <div class="bg-white dark:bg-gray-800 p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      In ogni gruppo indica con una X la parola che NON è sinonimo di quella evidenziata.
    </h2>

    <% synonym_groups = [
      { word: "dubbio", options: ["incertezza", "notizia", "perplessità"], non_synonym: "notizia" },
      { word: "facile", options: ["chiaro", "semplice", "conosciuto"], non_synonym: "conosciuto" },
      { word: "capire", options: ["scegliere", "comprendere", "intuire"], non_synonym: "scegliere" },
      { word: "evidente", options: ["ovvio", "paziente", "lampante"], non_synonym: "paziente" }
    ] %>

    <div class="space-y-3">
      <% synonym_groups.each_with_index do |group, group_index| %>
        <div class="rounded-lg p-3">
          <p class="text-gray-700 dark:text-gray-200 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= group[:word] %> →</p>
          <div class="flex flex-wrap gap-4 ml-6">
            <% group[:options].each do |option| %>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox"
                       name="synonym_group<%= group_index %>"
                       value="<%= option %>"
                       data-correct-answer="<%= group[:non_synonym] == option ? 'true' : 'false' %>"
                       class="w-5 h-5">
                <span><%= option %></span>
              </label>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 4: Identify antonyms -->
  <div class="bg-white dark:bg-gray-800 p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">4</span>
      In ogni gruppo indica con una X il contrario della parola evidenziata.
    </h2>

    <% antonym_groups = [
      { word: "secco", options: ["asciutto", "arido", "umido"], antonym: "umido" },
      { word: "tirare", options: ["trascinare", "spingere", "tendere"], antonym: "spingere" },
      { word: "raro", options: ["eccezionale", "straordinario", "frequente"], antonym: "frequente" },
      { word: "apposta", options: ["volutamente", "involontariamente", "intenzionalmente"], antonym: "involontariamente" }
    ] %>

    <div class="space-y-3">
      <% antonym_groups.each_with_index do |group, group_index| %>
        <div class="rounded-lg p-3">
          <p class="text-gray-700 dark:text-gray-200 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= group[:word] %> →</p>
          <div class="flex flex-wrap gap-4 ml-6">
            <% group[:options].each do |option| %>
              <label class="flex items-center gap-2 cursor-pointer">
                <input type="checkbox"
                       name="antonym_group<%= group_index %>"
                       value="<%= option %>"
                       data-correct-answer="<%= group[:antonym] == option ? 'true' : 'false' %>"
                       class="w-5 h-5">
                <span><%= option %></span>
              </label>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 5: Polysemic word "pianta" -->
  <div class="bg-white dark:bg-gray-800 p-4 md:p-6 pb-8">
    <h2 class="font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">5</span>
      La parola "pianta" è polisemica. Scrivi due frasi usandola con significati diversi.
    </h2>

    <div class="space-y-3">
      <div class="rounded-lg p-3">
        <span class="text-gray-700 dark:text-gray-200"><span class="text-<%= @pagina.base_color %>-500">•</span> 1. .................................................................</span>
      </div>
      <div class="rounded-lg p-3">
        <span class="text-gray-700 dark:text-gray-200"><span class="text-<%= @pagina.base_color %>-500">•</span> 2. .................................................................</span>
      </div>
    </div>
  </div>

  <div class="text-right text-gray-500 dark:text-gray-400 mb-6">
    Esercizi p. <%= link_to "144", pagina_path("nvl5_gram_p144"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
</div>
