<div class="max-w-6xl mx-auto bg-white p-3 md:p-6"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">
    <!-- Box introduttivo -->
    <div class="p-4 bg-pink-light rounded-lg">
      <p class="text-gray-700">
        Per avere un bancomat o una carta di debito o una carta di credito, bisogna avere un conto corrente in una banca. La <span class="font-bold">banca</span> è un'azienda che si occupa di gestire il denaro per i propri clienti.<br>
        Ma che cos'è il conto corrente?
      </p>
    </div>

    <!-- Layout 2 colonne -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Colonna sinistra: Definizione conto corrente -->
      <div class="p-4 bg-custom-blue rounded-lg">
        <p class="text-gray-700">
          <span class="text-<%= @pagina.base_color %>-500">&#9654;</span>
          Il <span class="font-bold">conto corrente</span> è un servizio offerto dalle banche: è una specie di salvadanaio per adulti, dove in genere viene depositato lo stipendio e vengono custoditi i risparmi. Sul conto corrente quindi si trova il denaro che via via viene utilizzato per le spese e quello che viene risparmiato.
        </p>
      </div>

      <!-- Colonna destra: Placeholder per immagine banca -->
      <div class="flex justify-center items-center">
        <div class="w-32 h-24 bg-gray-100 rounded-lg flex items-center justify-center">
          <span class="text-4xl">🏦</span>
        </div>
      </div>
    </div>

    <!-- Immagine vignetta banca -->
    <div class="flex justify-center">
      <%= image_tag "nvi5_mat/p173/p173_01.jpg", class: "max-w-lg rounded-lg shadow-md", alt: "Vignetta banca" %>
    </div>

    <!-- Interesse -->
    <div class="p-4 bg-pink-light rounded-lg">
      <p class="text-gray-700">
        <span class="text-<%= @pagina.base_color %>-500">&#9654;</span>
        Avere un conto corrente ha un <span class="font-bold">costo</span>, perché la banca si fa pagare per tenere al sicuro il nostro denaro. Se però si affidano alla banca i propri risparmi, c'è anche un "guadagno": la banca infatti paga un <span class="font-bold">interesse</span>, cioè dà dei soldi in più, a chi <span class="font-bold text-custom-pink">investe</span> il proprio denaro, cioè lo affida alla banca per un periodo di tempo più o meno lungo senza utilizzarlo.
      </p>
    </div>

    <!-- Definizione investire -->
    <div class="p-4 bg-white rounded-2xl border-3 border-<%= @pagina.base_color %>-400">
      <p class="text-gray-700">
        <span class="text-<%= @pagina.base_color %>-500">&#9654;</span>
        <span class="font-bold text-custom-pink">Investire</span>: significa impegnare i propri risparmi in modo che rendano ulteriori soldi.
      </p>
    </div>

    <!-- Esercizio -->
    <div class="p-4 bg-custom-pink/10 rounded-2xl border-3 border-custom-pink">
      <p class="text-gray-700 mb-4">
        <span class="text-<%= @pagina.base_color %>-500">•</span>
        Osserva la vignetta. Quanto guadagnerà la cliente investendo il suo denaro in banca?<br>
        Calcola il 2% di <span class="whitespace-nowrap">2 500</span> euro.
      </p>

      <div class="flex items-center gap-2 flex-wrap">
        <span class="text-gray-700">Guadagno:</span>
        <input type="text" data-correct-answer="50" class="w-20 border-b-2 border-dotted border-gray-400 text-center font-bold bg-transparent">
        <span class="shrink-0">euro</span>
      </div>
    </div>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
