<div class="max-w-6xl mx-auto bg-white p-3 md:p-6"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Introduzione -->
    <div class="text-center">
      <p class="italic text-<%= @pagina.base_color %>-600 font-bold">
        Gli Egizi usavano segni e simboli per contare e fare i calcoli.<br>
        Scopriamo insieme come rappresentavano i numeri e le frazioni.
      </p>
    </div>

    <!-- Sezione 1: I geroglifici per i numeri -->
    <div class="space-y-4">
      <div class="bg-custom-blue rounded-2xl p-4">
        <p class="text-gray-700">
          <%= numero_esercizio_badge(1) %>
          Gli Egizi rappresentavano con alcuni geroglifici i numeri <span class="font-bold text-<%= @pagina.base_color %>-600">1</span>, <span class="font-bold text-<%= @pagina.base_color %>-600">10</span>, <span class="font-bold text-<%= @pagina.base_color %>-600">100</span>, <span class="font-bold text-<%= @pagina.base_color %>-600">1000</span>... fino a <span class="font-bold text-<%= @pagina.base_color %>-600">un milione</span>.
        </p>
      </div>

      <!-- Immagine dei geroglifici -->
      <div class="flex justify-center">
        <%= image_tag "nvi4_mat/p018/p018_02.jpg", alt: "Geroglifici egizi", class: "max-w-full h-auto rounded-lg" %>
      </div>
    </div>

    <!-- Spiegazione sequenza -->
    <div class="px-4">
      <p class="text-gray-700 mb-4">
        <span class="text-<%= @pagina.base_color %>-500">▼</span>
        per scrivere un certo numero, i geroglifici venivano ripetuti in sequenza: questo è il numero <span class="font-bold text-<%= @pagina.base_color %>-600">21 123</span>
      </p>

      <!-- Immagine della sequenza -->
      <div class="flex justify-center">
        <%= image_tag "nvi4_mat/p018/output-1715932804018_132_778_841_109.png", alt: "Sequenza geroglifici 21123", class: "max-w-full h-auto" %>
      </div>
    </div>

    <!-- Sezione 2: Le frazioni -->
    <div class="space-y-4">
      <div class="bg-custom-blue rounded-2xl p-4">
        <p class="text-gray-700">
          <%= numero_esercizio_badge(2) %>
          Sapevano rappresentare anche le <span class="font-bold text-<%= @pagina.base_color %>-600">frazioni</span>, con cui misuravano una parte di un intero, per esempio una certa quantità di cereali o altri prodotti destinati al commercio.
        </p>
      </div>

      <!-- Immagine delle frazioni -->
      <div class="flex justify-center">
        <%= image_tag "nvi4_mat/p018/p018_03.jpg", alt: "Frazioni egizie", class: "max-w-full h-auto rounded-lg" %>
      </div>
    </div>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
