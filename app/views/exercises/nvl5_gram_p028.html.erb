<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Introduction -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <p class="text-base md:text-lg text-gray-700 mb-3">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Le parole con forme diverse ma significato simile sono <strong>sinonimi</strong> → <em>silenzioso / taciturno</em>.
    </p>
    <p class="text-base md:text-lg text-gray-700 mb-4">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Le parole con significati opposti sono <strong>contrari</strong> → <em>silenzioso / chiacchierone</em>.
    </p>
    <p class="text-base md:text-lg text-gray-700 mb-2">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Sinonimi e contrari sono presenti nelle varie parti del discorso:
    </p>
    <ul class="list-none space-y-2 text-base md:text-lg text-gray-700 ml-4">
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <strong>nomi</strong> → casa / abitazione, felicità / infelicità</li>
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <strong>aggettivi</strong> → famoso / noto, lungo / corto</li>
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <strong>verbi</strong> → sussurrare / bisbigliare, entrare / uscire</li>
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <strong>avverbi</strong> → velocemente / rapidamente, sopra / sotto</li>
    </ul>
  </div>

  <!-- Exercise 1: Color synonyms -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Colora allo stesso modo i gruppi di tre sinonimi.
    </h2>

    <p class="text-sm md:text-base text-gray-600 mb-4">
      Raggruppa le parole che hanno lo stesso significato:
    </p>

    <div class="grid grid-cols-2 sm:grid-cols-3 gap-2 text-base md:text-lg mb-4">
      <div class="bg-blue-100 rounded-lg px-3 py-3 text-center font-semibold text-blue-700">stanco</div>
      <div class="bg-red-100 rounded-lg px-3 py-3 text-center font-semibold text-red-700">perfido</div>
      <div class="bg-green-100 rounded-lg px-3 py-3 text-center font-semibold text-green-700">stupendo</div>
      <div class="bg-blue-100 rounded-lg px-3 py-3 text-center font-semibold text-blue-700">distrutto</div>
      <div class="bg-green-100 rounded-lg px-3 py-3 text-center font-semibold text-green-700">favoloso</div>
      <div class="bg-red-100 rounded-lg px-3 py-3 text-center font-semibold text-red-700">cattivo</div>
      <div class="bg-yellow-100 rounded-lg px-3 py-3 text-center font-semibold text-yellow-700">tenero</div>
      <div class="bg-yellow-100 rounded-lg px-3 py-3 text-center font-semibold text-yellow-700">soffice</div>
      <div class="bg-red-100 rounded-lg px-3 py-3 text-center font-semibold text-red-700">crudele</div>
      <div class="bg-yellow-100 rounded-lg px-3 py-3 text-center font-semibold text-yellow-700">morbido</div>
      <div class="bg-green-100 rounded-lg px-3 py-3 text-center font-semibold text-green-700">meraviglioso</div>
      <div class="bg-blue-100 rounded-lg px-3 py-3 text-center font-semibold text-blue-700">esausto</div>
    </div>

    <div class="text-sm text-gray-600 bg-gray-50 rounded-lg p-3">
      <p class="mb-2"><strong>Gruppi:</strong></p>
      <p class="mb-1"><span class="text-blue-500">•</span> Stanco, distrutto, esausto</p>
      <p class="mb-1"><span class="text-red-500">•</span> Perfido, cattivo, crudele</p>
      <p class="mb-1"><span class="text-green-500">•</span> Stupendo, favoloso, meraviglioso</p>
      <p><span class="text-yellow-500">•</span> Tenero, soffice, morbido</p>
    </div>
  </div>

  <!-- Exercise 2: Write synonyms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Per ogni parola scrivi un sinonimo.
    </h2>

    <% synonyms = {
      "sbaglio" => "errore",
      "strada" => "via",
      "parlare" => "dire",
      "rumore" => "suono",
      "dire" => "affermare"
    } %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% synonyms.each do |word, synonym| %>
        <div class="rounded-lg p-3 flex items-center justify-between gap-4">
          <div class="flex items-center gap-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="font-semibold text-gray-800"><%= word %></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
          </div>
          <input type="text"
                 class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= synonym %>"
                 data-correct-answer="<%= synonym %>"
                 maxlength="20">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 3: Write antonyms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      Per ogni parola scrivi un contrario.
    </h2>

    <% antonyms = {
      "ricordare" => "dimenticare",
      "alta" => "bassa",
      "vecchio" => "nuovo",
      "salire" => "scendere",
      "svelto" => "lento"
    } %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% antonyms.each do |word, antonym| %>
        <div class="rounded-lg p-3 flex items-center justify-between gap-4">
          <div class="flex items-center gap-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="font-semibold text-gray-800"><%= word %></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
          </div>
          <input type="text"
                 class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= antonym %>"
                 data-correct-answer="<%= antonym %>"
                 maxlength="20">
        </div>
      <% end %>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "141", pagina_path("nvl5_gram_p141"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
</div>
