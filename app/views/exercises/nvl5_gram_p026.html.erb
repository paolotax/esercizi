<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Introduction -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <p class="text-base md:text-lg text-gray-700 mb-3">
      <span class="text-<%= @pagina.base_color %>-500">•</span> La lingua italiana si modifica nel tempo.
    </p>
    <p class="text-base md:text-lg text-gray-700 mb-2">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Gli <strong>arcaismi</strong> sono le parole che non si usano più.
    </p>
    <p class="text-base md:text-lg text-gray-700">
      <span class="text-<%= @pagina.base_color %>-500">•</span> I <strong>neologismi</strong> sono le parole nuove.
    </p>
  </div>

  <!-- Exercise 1: Match archaisms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Collega ogni arcaismo al termine che usiamo oggi.
    </h2>

    <% archaisms = {
      "alterco" => "discussione",
      "stamani" => "questa mattina",
      "rammentare" => "ricordare",
      "pizzicagnolo" => "salumiere",
      "facondo" => "chiacchierone"
    } %>

    <% modern_terms = ["discussione", "questa mattina", "ricordare", "salumiere", "chiacchierone"] %>

    <div class="space-y-3 text-base md:text-lg">
      <% archaisms.each do |old_word, modern| %>
        <label class="flex flex-col sm:flex-row sm:items-center gap-2 rounded-lg p-3">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <span class="font-semibold text-gray-800 sm:w-1/3"><%= old_word %></span>
          <select class="flex-1 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 rounded-lg px-3 py-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                  data-correct-answer="<%= modern %>">
            <option value="">Scegli...</option>
            <% modern_terms.each do |term| %>
              <option value="<%= term %>"><%= term %></option>
            <% end %>
          </select>
        </label>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Identify word groups -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Completa con una X la frase che descrive il gruppo di parole.
    </h2>

    <div class="space-y-4 text-base md:text-lg">
      <div class="rounded-lg p-3">
        <p class="font-semibold text-gray-700 mb-3"><span class="text-<%= @pagina.base_color %>-500">•</span> balocco • dardo • desio • rio</p>
        <p class="text-gray-700 mb-2">Queste parole sono:</p>
        <div class="flex gap-4">
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="group1" value="arcaismi" data-correct-answer="arcaismi" class="w-5 h-5">
            <span>arcaismi</span>
          </label>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="group1" value="neologismi" class="w-5 h-5">
            <span>neologismi</span>
          </label>
        </div>
      </div>

      <div class="rounded-lg p-3">
        <p class="font-semibold text-gray-700 mb-3"><span class="text-<%= @pagina.base_color %>-500">•</span> digitalizzare • termoarredo • computer</p>
        <p class="text-gray-700 mb-2">Queste parole sono:</p>
        <div class="flex gap-4">
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="group2" value="arcaismi" class="w-5 h-5">
            <span>arcaismi</span>
          </label>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="radio" name="group2" value="neologismi" data-correct-answer="neologismi" class="w-5 h-5">
            <span>neologismi</span>
          </label>
        </div>
      </div>
    </div>
  </div>

  <!-- GIOCO: Translate archaic sentence -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">🎮</span>
      Leggi questa frase piena di arcaismi.
    </h2>

    <p class="text-xl md:text-2xl font-bold text-center text-<%= @pagina.base_color %>-700 mb-4 bg-<%= @pagina.base_color %>-50 rounded-xl py-4 px-3">
      Il probo vegliardo desinava al suo desco.
    </p>

    <p class="text-base md:text-lg text-gray-700 mb-4">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Secondo te, come può essere "tradotta" nell'italiano dei nostri giorni?
    </p>

    <div class="space-y-3 text-base md:text-lg">
      <label class="flex items-start gap-3 bg-<%= @pagina.base_color %>-50 p-3 rounded-lg cursor-pointer hover:bg-<%= @pagina.base_color %>-100 transition">
        <input type="radio" name="translation" value="a" class="w-5 h-5 mt-1">
        <span>L'astuto marinaio scriveva a suo figlio.</span>
      </label>
      <label class="flex items-start gap-3 bg-<%= @pagina.base_color %>-50 p-3 rounded-lg cursor-pointer hover:bg-<%= @pagina.base_color %>-100 transition">
        <input type="radio" name="translation" value="b" class="w-5 h-5 mt-1">
        <span>Il malvagio ragazzo dormiva nel suo letto.</span>
      </label>
      <label class="flex items-start gap-3 bg-<%= @pagina.base_color %>-50 p-3 rounded-lg cursor-pointer hover:bg-<%= @pagina.base_color %>-100 transition">
        <input type="radio" name="translation" value="c" data-correct-answer="c" class="w-5 h-5 mt-1">
        <span>Il buon vecchio pranzava al suo tavolo.</span>
      </label>
      <label class="flex items-start gap-3 bg-<%= @pagina.base_color %>-50 p-3 rounded-lg cursor-pointer hover:bg-<%= @pagina.base_color %>-100 transition">
        <input type="radio" name="translation" value="d" class="w-5 h-5 mt-1">
        <span>Il valoroso cavaliere cavalcava verso il suo castello.</span>
      </label>
    </div>

    <p class="text-sm md:text-base text-gray-600 mt-4 italic">
      Confrontati con le compagne e i compagni. Avete dato tutti la stessa risposta?
    </p>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "140", pagina_path("nvl5_gram_p140"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Section: Borrowed Words -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-3">
      Le parole in prestito
    </h2>
    <p class="text-base md:text-lg text-gray-700">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Nella lingua italiana sono presenti parole prese in prestito da varie lingue straniere e che sono diventate di uso comune.
    </p>
  </div>

  <!-- Exercise 1: Match English words -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Collega ogni parola inglese alla parola italiana corrispondente.
    </h2>

    <% english_words = {
      "basket" => "pallacanestro",
      "videogame" => "videogioco",
      "film" => "pellicola",
      "roast beef" => "arrosto di vitello",
      "e-mail" => "posta elettronica",
      "clown" => "pagliaccio",
      "party" => "festa",
      "weekend" => "fine settimana",
      "scooter" => "motorino"
    } %>

    <% italian_meanings = ["pallacanestro", "videogioco", "pellicola", "arrosto di vitello", "posta elettronica", "pagliaccio", "festa", "fine settimana", "motorino"] %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% english_words.each do |english, italian| %>
        <label class="flex flex-col rounded-lg p-3">
          <span class="font-semibold text-gray-800 mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= english %></span>
          <select class="border-b-2 border-dotted border-<%= @pagina.base_color %>-500 rounded-lg px-3 py-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                  data-correct-answer="<%= italian %>">
            <option value="">Scegli...</option>
            <% italian_meanings.each do |meaning| %>
              <option value="<%= meaning %>"><%= meaning %></option>
            <% end %>
          </select>
        </label>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Write Italian equivalents -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Scrivi la parola italiana che corrisponde a ciascuna delle seguenti parole straniere.
    </h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">
      Se hai bisogno di aiuto, consulta il dizionario.
    </p>

    <% foreign_words = {
      "boxe" => "pugilato",
      "toilette" => "bagno",
      "ticket" => "biglietto",
      "team" => "squadra",
      "foulard" => "fazzoletto",
      "chips" => "patatine"
    } %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% foreign_words.each do |foreign, italian| %>
        <div class="rounded-lg p-3 flex items-center justify-between gap-4">
          <div class="flex items-center gap-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="font-semibold text-gray-800"><%= foreign %></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
          </div>
          <input type="text"
                 class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= italian %>"
                 data-correct-answer="<%= italian %>"
                 maxlength="20">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 3: Group work -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      Lavora in coppia con un compagno o una compagna e scrivete almeno un'altra parola che l'italiano ha preso in prestito da queste lingue straniere.
    </h2>

    <% language_examples = {
      "dal francese" => "PELUCHE",
      "dal tedesco" => "KRAPFEN",
      "dal giapponese" => "KARATE",
      "dall'inglese" => "HOBBY"
    } %>

    <div class="space-y-3 text-base md:text-lg">
      <% language_examples.each do |lang, example| %>
        <div class="rounded-lg p-3 flex items-center justify-between gap-4">
          <div class="flex items-center gap-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="font-semibold text-gray-800"><%= lang %></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <span class="text-gray-700"><%= example %>,</span>
          </div>
          <input type="text"
                 class="w-32 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer=""
                 maxlength="30">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
</div>
