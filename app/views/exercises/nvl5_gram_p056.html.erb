<div class="max-w-6xl mx-auto p-3 md:p-8 bg-gradient-to-br from-lime-50 via-white to-green-50 rounded-3xl shadow-xl" data-controller="exercise-checker">

  <!-- Header -->
  <div class="mb-8">
    <div class="flex items-start sm:items-center justify-between gap-4 mb-4">
      <div class="flex flex-col-reverse sm:flex-row items-start sm:items-center gap-2 sm:gap-4">
        <div class="bg-lime-500 text-white text-md sm:text-xl px-2 sm:px-4 py-1.5 sm:py-2 rounded-lg font-bold">
          MORFOLOGIA
        </div>
        <h1 class="text-2xl sm:text-3xl font-bold text-lime-600">Aggettivi e pronomi possessivi</h1>
      </div>
      <div class="flex-shrink-0 bg-red-600 text-white rounded-full w-10 h-10 sm:w-12 sm:h-12 flex items-center justify-center font-bold text-lg sm:text-xl">
        56
      </div>
    </div>
  </div>

  <!-- Info Box with Illustration -->
  <div class="mb-6 bg-gradient-to-r from-yellow-100 to-amber-100 rounded-2xl border-4 border-yellow-300 shadow-2xl p-4 md:p-6">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center mb-4">
      <div>
        <%= image_tag "nvl5_gram_p056_immagine01.jpg", alt: "Illustrazione possessivi", class: "w-full rounded-lg shadow-lg" %>
      </div>
      <div>
        <p class="text-base md:text-lg text-gray-700 mb-3">
          I <strong>possessivi</strong> indicano a chi appartiene ciò di cui si parla.
        </p>
        <ul class="space-y-2 text-base md:text-lg text-gray-700">
          <li class="flex items-start gap-2">
            <span class="text-red-600 font-bold">•</span>
            <span>Gli <strong class="text-red-600">aggettivi possessivi</strong> accompagnano il nome → La <strong>mia</strong> maglietta è verde.</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-blue-600 font-bold">•</span>
            <span>I <strong class="text-blue-600">pronomi possessivi</strong> sostituiscono il nome → La <strong>mia</strong> (maglietta) è bianca.</span>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Exercise 1: IMPARARE TUTTI - Underline possessives -->
  <div class="mb-6 bg-gradient-to-r from-blue-100 to-cyan-100 rounded-2xl border-4 border-blue-300 shadow-2xl p-4 md:p-6" data-controller="word-highlighter" data-word-highlighter-multi-color-value="true">
    <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-4 flex items-center flex-wrap gap-2">
      <span class="bg-blue-600 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      <span class="bg-yellow-400 text-blue-900 px-3 py-1 rounded-lg font-bold">IMPARARE TUTTI</span>
      <span>Clicca sugli <span class="text-red-600">aggettivi possessivi<span class="inline-block w-6 h-6 bg-red-300 rounded ml-1 align-middle cursor-pointer transition transform hover:scale-110" data-word-highlighter-target="colorBox" data-color="red" data-action="click->word-highlighter#selectColor"></span></span> e sui <span class="text-blue-600">pronomi possessivi<span class="inline-block w-6 h-6 bg-blue-300 rounded ml-1 align-middle cursor-pointer transition transform hover:scale-110" data-word-highlighter-target="colorBox" data-color="blue" data-action="click->word-highlighter#selectColor"></span></span>.</span>
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-base md:text-lg cursor-pointer">
      <div class="bg-white rounded-lg p-4 space-y-3">
        <p class="text-gray-700">• Il <span class="transition" data-word-highlighter-target="word" data-correct="red" data-action="click->word-highlighter#toggleHighlight">mio</span> astuccio è sul banco, il <span class="transition" data-word-highlighter-target="word" data-correct="blue" data-action="click->word-highlighter#toggleHighlight">tuo</span> è ancora nello zaino.</p>
        <p class="text-gray-700">• Dove sono i <span class="transition" data-word-highlighter-target="word" data-correct="red" data-action="click->word-highlighter#toggleHighlight">vostri</span> libri? Questi sono i <span class="transition" data-word-highlighter-target="word" data-correct="blue" data-action="click->word-highlighter#toggleHighlight">nostri</span>.</p>
      </div>
      <div class="bg-white rounded-lg p-4 space-y-3">
        <p class="text-gray-700">• Il cane grosso è di Pablo, il <span class="transition" data-word-highlighter-target="word" data-correct="blue" data-action="click->word-highlighter#toggleHighlight">mio</span> è quello piccolo.</p>
        <p class="text-gray-700">• Farò il <span class="transition" data-word-highlighter-target="word" data-correct="red" data-action="click->word-highlighter#toggleHighlight">mio</span> dovere se lui farà il <span class="transition" data-word-highlighter-target="word" data-correct="blue" data-action="click->word-highlighter#toggleHighlight">suo</span>.</p>
      </div>
    </div>
  </div>

  <!-- Exercise 2: Volgi al plurale -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-green-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-green-700 mb-4">
      <span class="bg-green-600 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center mr-2">2</span>
      Volgi al plurale.
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-base md:text-lg">
      <% plural_items = [
        { singular: "La mia amica", plural: "Le mie amiche" },
        { singular: "Il tuo gatto", plural: "I tuoi gatti" },
        { singular: "La sua scarpa", plural: "Le sue scarpe" },
        { singular: "Il suo astuccio", plural: "I suoi astucci" },
        { singular: "Il nostro camper", plural: "I nostri camper" },
        { singular: "Il loro giornale", plural: "I loro giornali" }
      ] %>

      <% plural_items.each do |item| %>
        <div class="bg-green-50 border-2 border-green-200 rounded-xl px-4 py-3">
          <div class="flex items-center gap-3">
            <span class="text-gray-700 font-semibold"><%= item[:singular] %></span>
            <span class="text-gray-400">→</span>
            <input type="text"
                   class="border-b-2 border-green-500 bg-transparent px-2 py-1 text-left focus:outline-none focus:border-green-700 flex-1"
                   data-fill-blanks-target="input"
                   data-answer="<%= item[:plural] %>"
                   data-correct-answer="<%= item[:plural] %>"
                   placeholder="...........">
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 3: Sottolinea il pronome e indica il nome -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-purple-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-purple-700 mb-4">
      <span class="bg-purple-600 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center mr-2">3</span>
      Sottolinea il pronome possessivo e scrivi il nome che sostituisce.
    </h2>

    <div class="space-y-4 text-base md:text-lg">
      <% pronoun_sentences = [
        { sentence: "Il mio libro è bello e colorato, mentre il <strong>tuo</strong> è noioso.", answer: "libro" },
        { sentence: "Mi presti la penna? Ho perso la <strong>mia</strong>.", answer: "penna" },
        { sentence: "Il nostro viaggio è saltato, spero che riusciate a fare il <strong>vostro</strong>.", answer: "viaggio" }
      ] %>

      <% pronoun_sentences.each do |item| %>
        <div class="bg-purple-50 border-2 border-purple-200 rounded-xl px-4 py-3">
          <div class="flex flex-col md:flex-row md:items-center gap-3">
            <p class="text-gray-700 flex-1"><%= raw item[:sentence] %></p>
            <div class="flex items-center gap-2">
              <span class="text-gray-500">Nome:</span>
              <input type="text"
                     class="border-b-2 border-purple-500 bg-transparent px-2 py-1 text-center focus:outline-none focus:border-purple-700 w-32"
                     data-fill-blanks-target="input"
                     data-answer="<%= item[:answer] %>"
                     data-correct-answer="<%= item[:answer] %>"
                     placeholder="........">
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 4: Sottolinea la forma corretta -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-orange-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-orange-700 mb-4">
      <span class="bg-orange-600 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center mr-2">4</span>
      Sottolinea la forma corretta.
    </h2>

    <div class="bg-cyan-50 rounded-lg p-4 mb-4">
      <p class="text-base md:text-lg text-gray-700">
        Sono aggettivi e pronomi possessivi anche <strong class="text-red-600">altrui</strong> e <strong class="text-red-600">proprio</strong>.
      </p>
    </div>

    <div class="space-y-3 text-base md:text-lg" data-controller="multiple-choice">
      <% correct_forms = [
        { text: "Non invidiare le fortune", options: ["proprie", "altrui"], correct: "altrui" },
        { text: "Bisogna rispettare le opinioni di tutti, anche quelle", options: ["proprie", "altrui"], correct: "altrui" },
        { text: "Ognuno valuti i", options: ["propri", "altrui"], correct: "propri", continuation: "limiti prima di guardare le difficoltà del prossimo." }
      ] %>

      <% correct_forms.each_with_index do |item, index| %>
        <div class="bg-orange-50 border-2 border-orange-200 rounded-xl px-4 py-3">
          <p class="text-gray-700">
            • <%= item[:text] %>
            <% item[:options].each do |option| %>
              <label class="inline-flex items-center gap-2 mx-2 cursor-pointer">
                <input type="radio"
                       name="exercise4_<%= index %>"
                       value="<%= option %>"
                       class="w-4 h-4"
                       data-multiple-choice-target="input"
                       data-answer="<%= item[:correct] %>">
                <span class="font-semibold <%= option == item[:correct] ? 'text-green-700' : '' %>"><%= option %></span>
              </label>
            <% end %>
            <%= item[:continuation] if item[:continuation] %>
          </p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Reference -->
  <div class="mt-6 text-center">
    <p class="text-red-600 font-bold text-base md:text-lg">Esercizi p. <%= link_to "158", pagina_path("nvl5_gram_p158"), class: "text-gray-600 hover:text-gray-800 underline" %></p>
  </div>

  <%= render partial: "shared/exercise_controls" %>

</div>
