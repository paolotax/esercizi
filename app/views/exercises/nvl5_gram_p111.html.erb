<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Info Box -->
  <div class="bg-<%= @pagina.base_color %>-50 p-4 md:p-6 pb-8 rounded-lg">
    <div class="flex flex-col lg:flex-row gap-6 items-start">
      <div class="flex-1">
        <ul class="space-y-3 text-base md:text-lg text-gray-700">
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <span>I <strong>complementi di tempo</strong> sono complementi indiretti;</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <span>indicano il tempo in cui si svolge l'azione espressa dal predicato;</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500 font-bold">•</span>
            <div>
              <span>rispondono alle domande: <strong>quando?</strong> / <strong>per quanto tempo?</strong> / <strong>da quanto tempo?</strong> / <strong>tra quanto tempo?</strong></span>
              <div class="mt-2 space-y-1 text-sm italic">
                <div>→ Mi sono svegliata (<em>quando?</em>) <strong class="text-purple-600">alle sei</strong>.</div>
                <div>→ Non vedevo l'alba (<em>da quanto tempo?</em>) <strong class="text-purple-600">da molti anni</strong>.</div>
              </div>
            </div>
          </li>
        </ul>
      </div>
      <!-- Diario -->
      <div class="flex-shrink-0">
        <div class="bg-white border-2 border-<%= @pagina.base_color %>-300 rounded-lg p-4 shadow-lg transform rotate-2">
          <div class="text-<%= @pagina.base_color %>-600 italic text-lg mb-2">Caro diario,</div>
          <div class="text-gray-700 italic text-sm">
            questa mattina mi sono<br>
            svegliata <span class="text-<%= @pagina.base_color %>-600 font-bold">alle sei</span>... Non<br>
            vedevo l'alba <span class="text-<%= @pagina.base_color %>-600 font-bold">da molti anni</span>!
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Exercise 1: IMPARARE TUTTI -->
  <div class="bg-<%= @pagina.base_color %>-100 p-4 md:p-6 pb-8 rounded-lg">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      <span class="bg-yellow-400 text-blue-900 px-3 py-1 rounded-lg font-bold">IMPARARE TUTTI</span>
      Dividi la frase in sintagmi, sottolinea il complemento di tempo e scrivi a quale domanda risponde.
    </h2>

    <div class="space-y-3 text-base md:text-lg">
      <!-- Esempio -->
      <div class="bg-white rounded-lg p-4 border-2 border-green-400">
        <div class="flex items-start gap-2">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <div class="flex-1">
            <div class="mb-2" data-controller="word-highlighter">
              <span class="bg-purple-200 px-1 rounded">Ogni estate</span>
              <strong>/</strong> affitto <strong>/</strong> una casa <strong>/</strong> al mare.
            </div>
            <div class="text-green-600 font-bold">Quando?</div>
          </div>
        </div>
      </div>

      <% frases_tempo = [
        { text: "Abbiamo aspettato per ore la telefonata.", complement: "per ore", answer: "per quanto tempo?" },
        { text: "Non vedo Cesare dall'inverno scorso.", complement: "dall'inverno scorso", answer: "da quanto tempo?" },
        { text: "Ci vediamo tra mezz'ora in piazza.", complement: "tra mezz'ora", answer: "tra quanto tempo?" }
      ] %>

      <% frases_tempo.each do |frase| %>
        <div class="bg-white rounded-lg p-4">
          <div class="flex items-start gap-2">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <div class="flex-1">
              <div class="mb-2" data-controller="word-highlighter">
                <% words = frase[:text].split(/\s+/) %>
                <% words.each_with_index do |word, i| %>
                  <% if frase[:complement].split(/\s+/).include?(word) %>
                    <span class="transition cursor-pointer hover:bg-purple-100 px-1 rounded"
                          data-word-highlighter-target="word"
                          data-correct="true"
                          data-action="click->word-highlighter#toggleHighlight"><%= word %></span>
                  <% else %>
                    <%= word %>
                  <% end %>
                  <%= " " unless i == words.length - 1 %>
                <% end %>
              </div>
              <div data-controller="fill-blanks">
                <input type="text"
                       class="w-full md:w-96 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 focus:outline-none"
                       data-fill-blanks-target="input"
                       data-correct-answer="<%= frase[:answer] %>">
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2 -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Completa le frasi con un complemento di tempo adatto.
    </h2>

    <div class="space-y-3 text-base md:text-lg">
      <% [
        "ho appuntamento con il parrucchiere.",
        "La nonna resta con noi",
        "frequenterò la prima media.",
        "faccio colazione."
      ].each do |fine_frase| %>
        <div class="bg-<%= @pagina.base_color %>-50 rounded-lg p-4">
          <div class="flex flex-wrap items-center gap-2">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <input type="text"
                   class="w-48 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 focus:outline-none"
                   data-fill-blanks-target="input"
                   data-correct-answer="libera">
            <span class="text-gray-700"><%= fine_frase %></span>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 3 -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      Indica con una X la frase in cui il sintagma evidenziato è un complemento di tempo.
    </h2>

    <div class="space-y-3 text-base md:text-lg">
      <% [
        { text: "Il treno arriverà alle dieci", highlight: "di mattina", is_tempo: true },
        { text: "Papà prepara", highlight: "dei biscotti", extra: "per voi.", is_tempo: false },
        { text: "Questa sera Michele mangia", highlight: "a casa", is_tempo: false },
        { text: "Ci vediamo", highlight: "alle sei", extra: "ai giardinetti.", is_tempo: true }
      ].each do |item| %>
        <label class="flex items-center gap-3 bg-<%= @pagina.base_color %>-50 rounded-lg p-4 cursor-pointer hover:bg-<%= @pagina.base_color %>-100">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <span class="flex-1 text-gray-700">
            <%= item[:text] %> <strong><%= item[:highlight] %></strong><%= item[:extra] if item[:extra] %>
          </span>
          <input type="checkbox"
                 class="w-6 h-6 border-2 border-gray-400 rounded focus:ring-2 focus:ring-<%= @pagina.base_color %>-500"
                 data-correct-answer="<%= item[:is_tempo] %>">
        </label>
      <% end %>
    </div>
  </div>

  <!-- Reference -->
  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "185", pagina_path("nvl5_gram_p185"), class: "text-gray-600 hover:text-gray-800 underline" %> e <%= link_to "187", pagina_path("nvl5_gram_p187"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <%= render 'shared/exercise_controls' %>

</div>
