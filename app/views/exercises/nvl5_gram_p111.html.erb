<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" data-controller="exercise-checker">

  <!-- Header con badge SINTASSI -->
  <div class="mb-8">
    <div class="flex items-start justify-between">
      <!-- Badge e Titolo -->
      <div class="flex items-start gap-4">
        <!-- Badge SINTASSI con icona lampadina -->
        <div class="bg-yellow-400 text-orange-700 px-6 py-3 rounded-lg font-bold text-lg flex items-center gap-2">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
            <path d="M10 2a6 6 0 00-6 6c0 1.887.875 3.517 2.084 4.485A.5.5 0 016 12.5V14a1 1 0 001 1h6a1 1 0 001-1v-1.5a.5.5 0 01-.084-.015C15.125 11.517 16 9.887 16 8a6 6 0 00-6-6z"/>
          </svg>
          SINTASSI
        </div>
      </div>
      <!-- Numero pagina -->
      <div class="bg-orange-500 text-white rounded-full w-12 h-12 flex items-center justify-center font-bold text-xl">
        111
      </div>
    </div>

    <!-- Titolo principale -->
    <h1 class="text-3xl font-bold text-orange-600 mt-6 text-center">
      I complementi di tempo
    </h1>
  </div>

  <!-- Box teoria con sfondo beige e illustrazione -->
  <div class="bg-yellow-50 border-2 border-yellow-300 rounded-lg p-6 mb-8">
    <div class="flex flex-col lg:flex-row gap-6 items-start">
      <!-- Teoria testuale -->
      <div class="flex-1">
        <ul class="space-y-3 text-gray-700">
          <li class="flex">
            <span class="text-gray-500 mr-2">•</span>
            <span>I <strong class="underline">complementi di tempo</strong> sono complementi indiretti;</span>
          </li>
          <li class="flex">
            <span class="text-gray-500 mr-2">•</span>
            <span>indicano il tempo in cui si svolge l'azione espressa dal predicato;</span>
          </li>
          <li class="flex">
            <span class="text-gray-500 mr-2">•</span>
            <div>
              <span>rispondono alle domande: </span>
              <strong class="underline">quando?</strong> /
              <strong class="underline">per quanto tempo?</strong> /
              <strong class="underline">da quanto tempo?</strong> /
              <strong class="underline">tra quanto tempo?</strong>
              <div class="mt-2 space-y-1">
                <div>→ Mi sono svegliata (<em>quando?</em>) <strong class="underline text-purple-600">alle sei</strong>.</div>
                <div>→ Non vedevo l'alba (<em>da quanto tempo?</em>) <strong class="underline text-purple-600">da molti anni</strong>.</div>
              </div>
            </div>
          </li>
        </ul>
      </div>

      <!-- Illustrazione diario -->
      <div class="flex-shrink-0">
        <div class="bg-white border-4 border-purple-300 rounded-lg p-4 shadow-lg transform rotate-2">
          <div class="text-purple-600 italic text-lg mb-2">Caro diario,</div>
          <div class="text-gray-700 italic">
            questa mattina mi sono<br>
            svegliata <span class="text-purple-600 font-bold">alle sei</span>... Non<br>
            vedevo l'alba <span class="text-purple-600 font-bold">da molti anni</span>!
          </div>
          <div class="text-right mt-2">
            <svg class="inline w-8 h-8 text-red-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd"/>
            </svg>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Esercizio 1 - IMPARARE TUTTI con word-highlighter -->
  <div class="bg-yellow-100 border-2 border-yellow-400 rounded-lg p-6 mb-8">
    <div class="flex items-center gap-3 mb-4">
      <div class="bg-blue-500 text-white rounded-full w-10 h-10 flex items-center justify-center font-bold text-xl">
        1
      </div>
      <div class="bg-blue-500 text-white px-4 py-2 rounded-lg font-bold">
        IMPARARE TUTTI
      </div>
    </div>

    <div class="mb-4">
      <p class="font-bold text-gray-700">
        Dividi la frase in sintagmi, sottolinea il <span class="underline">complemento di tempo</span>
        e scrivi a quale domanda risponde. Osserva l'esempio.
      </p>
    </div>

    <div class="space-y-4">
      <!-- Esempio -->
      <div class="bg-white p-4 rounded-lg border-2 border-green-400">
        <div class="flex items-start gap-2">
          <span class="text-gray-500">•</span>
          <div class="flex-1">
            <div class="mb-2" data-controller="word-highlighter">
              <span class="bg-purple-200 px-1 rounded">Ogni estate</span>
              <strong>/</strong> affitto <strong>/</strong> una casa <strong>/</strong> al mare.
            </div>
            <div class="text-green-600 font-bold">Quando?</div>
          </div>
        </div>
      </div>

      <!-- Frasi da completare -->
      <% frases_tempo = [
        { text: "Abbiamo aspettato per ore la telefonata.", complement: "per ore", answer: "per quanto tempo?" },
        { text: "Non vedo Cesare dall'inverno scorso.", complement: "dall'inverno scorso", answer: "da quanto tempo?" },
        { text: "Ci vediamo tra mezz'ora in piazza.", complement: "tra mezz'ora", answer: "tra quanto tempo?" }
      ] %>

      <% frases_tempo.each_with_index do |frase, index| %>
        <div class="bg-white p-4 rounded-lg">
          <div class="flex items-start gap-2">
            <span class="text-gray-500">•</span>
            <div class="flex-1">
              <div class="mb-2" data-controller="word-highlighter">
                <% words = frase[:text].split(/\s+/) %>
                <% words.each_with_index do |word, i| %>
                  <% if frase[:complement].split(/\s+/).include?(word) %>
                    <span class="transition cursor-pointer hover:bg-purple-100 px-1 rounded"
                          data-word-highlighter-target="word"
                          data-correct="true"
                          data-action="click->word-highlighter#toggleHighlight"><%= word %></span>
                  <% else %>
                    <%= word %>
                  <% end %>
                  <%= " " unless i == words.length - 1 %>
                <% end %>
              </div>
              <div data-controller="fill-blanks">
                <input type="text"
                       class="w-full md:w-96 px-3 py-2 border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:outline-none"
                       data-fill-blanks-target="input"
                       data-correct-answer="<%= frase[:answer] %>">
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Esercizio 2 - Completa le frasi -->
  <div class="bg-white border-2 border-gray-300 rounded-lg p-6 mb-8" data-controller="fill-blanks">
    <div class="flex items-center gap-3 mb-4">
      <div class="bg-orange-500 text-white rounded-full w-10 h-10 flex items-center justify-center font-bold text-xl">
        2
      </div>
    </div>

    <p class="font-bold text-gray-700 mb-4">
      Completa le frasi con un <span class="underline">complemento di tempo</span> adatto.
    </p>

    <div class="space-y-3">
      <% [
        "ho appuntamento con il parrucchiere.",
        "La nonna resta con noi",
        "frequenterò la prima media.",
        "faccio colazione."
      ].each_with_index do |fine_frase, index| %>
        <div class="flex items-center gap-2">
          <span class="text-gray-500">•</span>
          <input type="text"
                 class="flex-1 max-w-xs px-3 py-2 border-b-2 border-dotted border-gray-400 focus:border-blue-500 focus:outline-none"
                 data-fill-blanks-target="input"
                 data-correct-answer="libera">
          <span><%= fine_frase %></span>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Esercizio 3 - Indica con una X -->
  <div class="bg-white border-2 border-gray-300 rounded-lg p-6 mb-8">
    <div class="flex items-center gap-3 mb-4">
      <div class="bg-orange-500 text-white rounded-full w-10 h-10 flex items-center justify-center font-bold text-xl">
        3
      </div>
    </div>

    <p class="font-bold text-gray-700 mb-4">
      Indica con una <span class="underline">X</span> la frase in cui il sintagma evidenziato è un <span class="underline">complemento di tempo</span>.
    </p>

    <div class="space-y-3">
      <% [
        { text: "Il treno arriverà alle dieci", highlight: "di mattina", is_tempo: true },
        { text: "Papà prepara", highlight: "dei biscotti", extra: "per voi.", is_tempo: false },
        { text: "Questa sera Michele mangia", highlight: "a casa", is_tempo: false },
        { text: "Ci vediamo", highlight: "alle sei", extra: "ai giardinetti.", is_tempo: true }
      ].each_with_index do |item, index| %>
        <label class="flex items-center gap-3 cursor-pointer hover:bg-gray-50 p-2 rounded">
          <span class="text-gray-500">•</span>
          <span>
            <%= item[:text] %> <strong><%= item[:highlight] %></strong><%= item[:extra] if item[:extra] %>
          </span>
          <input type="checkbox"
                 class="ml-auto w-6 h-6 border-2 border-gray-400 rounded focus:ring-2 focus:ring-blue-500"
                 data-correct-answer="<%= item[:is_tempo] %>">
        </label>
      <% end %>
    </div>
  </div>

  <!-- Riferimento esercizi aggiuntivi -->
  <div class="mt-8 text-center">
    <div class="inline-block bg-orange-100 border-2 border-orange-300 rounded-lg px-6 py-3">
      <p class="font-bold text-orange-700">
        Esercizi p. 185 e 187
      </p>
    </div>
  </div>

  <!-- Exercise controls -->
  <%= render 'shared/exercise_controls', color: 'orange' %>
</div>