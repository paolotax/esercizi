<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Box teoria: decimi, centesimi, millesimi con linee dei numeri -->
    <div class="p-4 md:p-6 bg-gradient-to-br from-blue-50 via-cyan-50 to-pink-50 dark:from-blue-900/20 dark:via-cyan-900/20 dark:to-pink-900/20 rounded-2xl space-y-8">

      <!-- UN DECIMO -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
        <div class="space-y-2">
          <p class="text-gray-700 dark:text-gray-200">
            <span class="inline-block w-3 h-3 bg-blue-500 rounded-full mr-2"></span>
            La parte della linea dei numeri compresa fra 0 e 1 è l'unità che è stata divisa in <strong>10 parti uguali</strong>.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Ogni parte vale <strong>un decimo</strong> (<strong>d</strong>) dell'intera unità.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Puoi scrivere un decimo come frazione: <%= render 'shared/frazione', num: 1, den: 10 %> o come numero con la virgola: <strong class="text-cyan-600 dark:text-cyan-400">0,1</strong>.
          </p>
          <p class="font-bold text-cyan-600 dark:text-cyan-400 mt-2">
            <span class="text-red-500">0 u</span> e <span class="text-blue-500">1 d</span>
          </p>
        </div>
        <div class="flex flex-col items-center gap-2">
          <!-- Frazione sopra -->
          <div class="flex items-center gap-1">
            <%= render 'shared/frazione', num: 1, den: 10 %>
          </div>
          <!-- Linea dei numeri 0-1 -->
          <div class="relative w-full max-w-xs">
            <div class="flex items-center">
              <span class="text-gray-700 dark:text-gray-200 mr-1">0</span>
              <div class="flex-1 h-1 bg-green-500 relative">
                <div class="absolute left-0 top-1/2 -translate-y-1/2 w-3 h-3 bg-blue-500 rounded-full"></div>
                <% 10.times do |i| %>
                  <div class="absolute top-0 h-3 w-0.5 bg-green-600" style="left: <%= (i + 1) * 10 %>%"></div>
                <% end %>
              </div>
              <span class="text-gray-700 dark:text-gray-200 ml-1">1</span>
            </div>
            <!-- Etichetta 0,1 -->
            <div class="absolute left-[10%] top-6 flex flex-col items-center">
              <span class="text-gray-500 dark:text-gray-400">↓</span>
              <span class="font-bold text-gray-700 dark:text-gray-200">0,1</span>
            </div>
          </div>
        </div>
      </div>

      <hr class="border-gray-300 dark:border-gray-600">

      <!-- UN CENTESIMO -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
        <div class="space-y-2">
          <p class="text-gray-700 dark:text-gray-200">
            <span class="inline-block w-3 h-3 bg-pink-500 rounded-full mr-2"></span>
            La parte della linea dei numeri compresa fra 0 e 0,1 è stata divisa in <strong>10 parti uguali</strong>.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Ogni parte vale <strong>un centesimo</strong> (<strong>c</strong>) dell'intera unità.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Puoi scrivere un centesimo come frazione: <%= render 'shared/frazione', num: 1, den: 100 %> o come numero con la virgola: <strong class="text-cyan-600 dark:text-cyan-400">0,01</strong>.
          </p>
          <p class="font-bold text-cyan-600 dark:text-cyan-400 mt-2">
            <span class="text-red-500">0 u</span> e <span class="text-blue-500">0 d</span> e <span class="text-green-500">1 c</span>
          </p>
        </div>
        <div class="flex flex-col items-center gap-2">
          <!-- Frazione sopra -->
          <div class="flex items-center gap-1">
            <%= render 'shared/frazione', num: 1, den: 100 %>
          </div>
          <!-- Linea dei numeri 0-0,1 -->
          <div class="relative w-full max-w-xs">
            <div class="flex items-center">
              <span class="text-gray-700 dark:text-gray-200 mr-1">0</span>
              <div class="flex-1 h-1 bg-cyan-500 relative">
                <div class="absolute left-0 top-1/2 -translate-y-1/2 w-3 h-3 bg-pink-500 rounded-full"></div>
                <% 10.times do |i| %>
                  <div class="absolute top-0 h-3 w-0.5 bg-cyan-600" style="left: <%= (i + 1) * 10 %>%"></div>
                <% end %>
              </div>
              <span class="text-gray-700 dark:text-gray-200 ml-1">0,1</span>
            </div>
            <!-- Etichetta 0,01 -->
            <div class="absolute left-[10%] top-6 flex flex-col items-center">
              <span class="text-gray-500 dark:text-gray-400">↓</span>
              <span class="font-bold text-gray-700 dark:text-gray-200">0,01</span>
            </div>
          </div>
        </div>
      </div>

      <hr class="border-gray-300 dark:border-gray-600">

      <!-- UN MILLESIMO -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
        <div class="space-y-2">
          <p class="text-gray-700 dark:text-gray-200">
            <span class="inline-block w-3 h-3 bg-orange-500 rounded-full mr-2"></span>
            La parte della linea dei numeri compresa fra 0 e 0,01 è stata divisa in <strong>10 parti uguali</strong>.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Ogni parte vale <strong>un millesimo</strong> (<strong>m</strong>) dell'intera unità.
          </p>
          <p class="text-gray-700 dark:text-gray-200">
            Puoi scrivere un millesimo come frazione: <%= render 'shared/frazione', num: 1, den: 1000 %> o come numero con la virgola: <strong class="text-cyan-600 dark:text-cyan-400">0,001</strong>.
          </p>
          <p class="font-bold text-cyan-600 dark:text-cyan-400 mt-2">
            <span class="text-red-500">0 u</span> e <span class="text-blue-500">0 d</span> e <span class="text-green-500">0 c</span> e <span class="text-purple-500">1 m</span>
          </p>
        </div>
        <div class="flex flex-col items-center gap-2">
          <!-- Frazione sopra -->
          <div class="flex items-center gap-1">
            <%= render 'shared/frazione', num: 1, den: 1000 %>
          </div>
          <!-- Linea dei numeri 0-0,01 -->
          <div class="relative w-full max-w-xs">
            <div class="flex items-center">
              <span class="text-gray-700 dark:text-gray-200 mr-1">0</span>
              <div class="flex-1 h-1 bg-pink-400 relative">
                <div class="absolute left-0 top-1/2 -translate-y-1/2 w-3 h-3 bg-orange-500 rounded-full"></div>
                <% 10.times do |i| %>
                  <div class="absolute top-0 h-3 w-0.5 bg-pink-500" style="left: <%= (i + 1) * 10 %>%"></div>
                <% end %>
              </div>
              <span class="text-gray-700 dark:text-gray-200 ml-1">0,01</span>
            </div>
            <!-- Etichetta 0,001 -->
            <div class="absolute left-[10%] top-6 flex flex-col items-center">
              <span class="text-gray-500 dark:text-gray-400">↓</span>
              <span class="font-bold text-gray-700 dark:text-gray-200">0,001</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Immagine delle linee dei numeri sovrapposte -->
    <div class="flex justify-center">
      <%= image_tag "nvi4_mat/p073/p073_01.jpg",
          class: "max-w-full h-auto dark:bg-white dark:rounded-lg dark:p-1",
          alt: "Linee dei numeri per decimi, centesimi e millesimi" %>
    </div>

    <!-- Sezione: Leggere i numeri con la virgola -->
    <div class="p-4 md:p-6">
      <h2 class="font-bold italic text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400 mb-4">Leggere i numeri con la virgola</h2>

      <p class="text-gray-700 dark:text-gray-200 mb-4">
        I numeri con la virgola si leggono in base alla frazione che rappresentano oppure dicendo "zero virgola...".
      </p>

      <div class="overflow-x-auto">
        <table class="w-full border-collapse">
          <tbody>
            <tr>
              <td class="p-2 text-gray-700 dark:text-gray-200">Il numero <strong class="text-cyan-600 dark:text-cyan-400">0,7</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">si legge <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">sette decimi</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">oppure <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">zero virgola sette</strong>.</td>
            </tr>
            <tr>
              <td class="p-2 text-gray-700 dark:text-gray-200">Il numero <strong class="text-cyan-600 dark:text-cyan-400">0,59</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">si legge <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">cinquantanove centesimi</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">oppure <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">zero virgola cinquantanove</strong>.</td>
            </tr>
            <tr>
              <td class="p-2 text-gray-700 dark:text-gray-200">Il numero <strong class="text-cyan-600 dark:text-cyan-400">0,126</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">si legge <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">centoventisei millesimi</strong></td>
              <td class="p-2 text-gray-700 dark:text-gray-200">oppure <strong class="text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">zero virgola centoventisei</strong>.</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- ESERCIZI -->
    <div class="p-4 md:p-6 bg-orange-100 dark:bg-orange-900/30 rounded-2xl">
      <p class="font-bold text-gray-700 dark:text-gray-200 mb-4">
        <%= numero_esercizio_badge(1) %>
        Sul quaderno scrivi, in lettere e in cifre, i numeri corrispondenti alle frazioni.
      </p>

      <!-- Frazioni da convertire -->
      <div class="flex flex-wrap justify-center gap-4 p-4 bg-amber-50 dark:bg-amber-900/30 rounded-lg">
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 2, den: 10 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 3, den: 100 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 4, den: 100 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 5, den: 10 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 9, den: 1000 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 7, den: 10 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 1, den: 100 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 3, den: 1000 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 4, den: 10 %>
        </div>
        <div class="flex flex-col items-center">
          <%= render 'shared/frazione', num: 2, den: 1000 %>
        </div>
      </div>
    </div>

    <%= render 'shared/quaderno_link', pagine: [209, 214] %>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
