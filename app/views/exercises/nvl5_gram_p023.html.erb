<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 text-black dark:text-white p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

  <!-- Compound words -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="font-bold text-gray-800 dark:text-gray-200 mb-3">Alcune parole sono composte da due nomi</h2>
    <p class="text-gray-600 dark:text-gray-400 mb-4">Osserva l'esempio e completa.</p>

    <p class="font-semibold text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400 mb-3">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Caporeparto = capo + reparto
    </p>

    <% compound_words = {
      "Cavolfiore" => ["cavol", "fiore"],
      "Melograno" => ["melo", "grano"],
      "Pescespada" => ["pesce", "spada"]
    } %>

    <div class="space-y-3 text-gray-800 dark:text-gray-200">
      <% compound_words.each do |word, parts| %>
        <p class="rounded-lg p-3 flex flex-wrap items-center gap-2 leading-relaxed">
          <span class="text-<%= @pagina.base_color %>-500">•</span>
          <span class="font-semibold"><%= word %>:</span>
          <input type="text"
                 class="border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white w-24 md:w-28 text-center focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= parts.first %>"
                 data-correct-answer="<%= parts.first %>"
                 maxlength="12">
          <span>+</span>
          <input type="text"
                 class="border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white w-24 md:w-28 text-center focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= parts.last %>"
                 data-correct-answer="<%= parts.last %>"
                 maxlength="12">
        </p>
      <% end %>
    </div>
  </div>

  <!-- CLIL -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8">
    <h2 class="font-bold text-gray-800 dark:text-gray-200 mb-3 flex items-center gap-2">
      <span class="">🇬🇧</span>
      CLIL – Shopping words
    </h2>
    <ul class="space-y-2 text-gray-700 dark:text-gray-300">
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <span class="font-semibold text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">Borsa</span> → <span class="italic">shopping bag</span></li>
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <span class="font-semibold text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">Carrello</span> → <span class="italic">shopping trolley</span></li>
      <li><span class="text-<%= @pagina.base_color %>-500">•</span> <span class="font-semibold text-<%= @pagina.base_color %>-600 dark:text-<%= @pagina.base_color %>-400">Cassa</span> → <span class="italic">check-out</span></li>
    </ul>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
  </div><!-- chiude space-y-6 -->
</div>

