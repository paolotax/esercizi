<div class="max-w-6xl mx-auto p-3 md:p-8 bg-gradient-to-br from-orange-50 via-white to-blue-50 rounded-3xl shadow-xl" data-controller="exercise-checker">

  <!-- Header -->
  <div class="mb-8">
    <div class="flex items-start sm:items-center justify-between gap-4 mb-4">
      <div class="flex flex-col-reverse sm:flex-row items-start sm:items-center gap-2 sm:gap-4">
        <div class="bg-orange-500 text-white text-md sm:text-xl px-2 sm:px-4 py-1.5 sm:py-2 rounded-lg font-bold">
          LESSICO
        </div>
        <h1 class="text-2xl sm:text-3xl font-bold text-orange-600">Lessico al supermercato</h1>
      </div>
      <div class="flex-shrink-0 bg-orange-500 text-white rounded-full w-10 h-10 sm:w-12 sm:h-12 flex items-center justify-center font-bold text-lg sm:text-xl">
        23
      </div>
    </div>
    <p class="text-sm md:text-base text-orange-700">
      Organizza le parole del reparto ortofrutta e scopri come si formano alcuni nomi composti.
    </p>
  </div>

  <!-- Categorize produce -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-orange-200 shadow-2xl p-4 md:p-6" data-controller="exercise-group">
    <h2 class="text-lg md:text-xl font-bold text-orange-700 mb-2">Nel reparto ortofrutta trovi verdura e frutta</h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">Segui l’esempio: <span class="font-semibold text-orange-600">Carote → Verdura</span>.</p>

    <% produce = {
      "Carote" => "Verdura",
      "Patate" => "Verdura",
      "Limoni" => "Frutta",
      "Banane" => "Frutta",
      "Pomodori" => "Verdura",
      "Mele" => "Frutta",
      "Insalata" => "Verdura",
      "Cavoli" => "Verdura",
      "Arance" => "Frutta",
      "Ananas" => "Frutta"
    } %>

    <% produce_options = ["Scegli...", "Verdura", "Frutta"] %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% produce.each do |item, category| %>
        <label class="flex flex-col bg-orange-50 border-2 border-orange-200 rounded-xl px-3 py-3 shadow-sm">
          <span class="font-semibold text-orange-700 mb-2"><%= item %></span>
          <select class="border-2 border-orange-300 rounded-lg px-3 py-2 focus:outline-none focus:border-orange-500"
                  data-correct-answer="<%= category %>">
            <% produce_options.each do |opt| %>
              <option value="<%= opt %>" <%= opt == category && item == "Carote" ? 'selected' : '' %>><%= opt %></option>
            <% end %>
          </select>
        </label>
      <% end %>
    </div>
  </div>

  <!-- Compound words -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-blue-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-3">Alcune parole sono composte da due nomi</h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">Osserva l’esempio e completa.</p>

    <p class="font-semibold text-blue-700 mb-3">Caporeparto = capo + reparto</p>

    <% compound_words = {
      "Cavolfiore" => ["cavo", "fiore"],
      "Melograno" => ["melo", "grano"],
      "Pescespada" => ["pesce", "spada"]
    } %>

    <div class="space-y-3 text-base md:text-lg">
      <% compound_words.each do |word, parts| %>
        <p class="bg-blue-50 border-2 border-blue-200 rounded-xl px-3 py-2 flex flex-wrap items-center gap-2 leading-relaxed">
          <span class="font-semibold text-blue-700"><%= word %>:</span>
          <input type="text"
                 class="border-b-2 border-blue-500 bg-transparent w-24 md:w-28 text-center focus:outline-none focus:border-blue-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= parts.first %>"
                 data-correct-answer="<%= parts.first %>"
                 maxlength="12">
          <span>+</span>
          <input type="text"
                 class="border-b-2 border-blue-500 bg-transparent w-24 md:w-28 text-center focus:outline-none focus:border-blue-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= parts.last %>"
                 data-correct-answer="<%= parts.last %>"
                 maxlength="12">
        </p>
      <% end %>
    </div>
  </div>

  <!-- CLIL -->
  <div class="bg-white rounded-2xl border-4 border-gray-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-gray-700 mb-3 flex items-center gap-2">
      <span class="text-xl">🇬🇧</span>
      CLIL – Shopping words
    </h2>
    <ul class="space-y-2 text-base md:text-lg text-gray-700">
      <li><span class="font-semibold text-blue-600">Borsa</span> → <span class="italic">shopping bag</span></li>
      <li><span class="font-semibold text-blue-600">Carrello</span> → <span class="italic">shopping trolley</span></li>
      <li><span class="font-semibold text-blue-600">Cassa</span> → <span class="italic">check-out</span></li>
    </ul>
  </div>

  <!-- Exercise Controls -->
  <div class="mt-6">
    <%= render 'shared/exercise_controls', color: 'orange', current_page: 'nvl5_gram_p023' %>
  </div>
</div>

