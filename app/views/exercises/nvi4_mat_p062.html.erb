<div class="max-w-6xl mx-auto bg-white p-3 md:p-6"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Sezione 1: Frazione propria (2/4) -->
    <div class="py-4">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <p class="text-gray-700 mb-4">
            Il papà ha comprato alcune pizze per suo figlio Samir e per i suoi amici. Samir mangia <%= render 'shared/frazione', num: 2, den: 4 %> di una pizza.
          </p>

          <p class="font-bold text-gray-700 mb-3">
            <span class="text-<%= @pagina.base_color %>-500">•</span> Rispondi.
          </p>

          <div class="space-y-3">
            <div class="flex items-center gap-4 flex-wrap">
              <span class="text-gray-700">Samir mangia l'intera pizza?</span>
              <div class="flex gap-2">
                <label class="relative flex items-center justify-center w-10 h-8 bg-white border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
                  <input type="radio" name="samir_intera" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="false">
                  <span>Sì</span>
                </label>
                <label class="relative flex items-center justify-center w-10 h-8 bg-white border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
                  <input type="radio" name="samir_intera" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="true">
                  <span>No</span>
                </label>
              </div>
            </div>

            <p class="text-gray-700">
              La frazione <%= render 'shared/frazione', num: 2, den: 4 %> è <span class="font-bold">minore dell'intero</span>.
            </p>
          </div>

          <p class="font-bold text-gray-700 mt-4 mb-3">
            <span class="text-<%= @pagina.base_color %>-500">•</span> Sistema sulla linea la frazione <%= render 'shared/frazione', num: 2, den: 4 %>.
          </p>

          <!-- Linea dei numeri 0-2 -->
          <div class="relative w-full max-w-md h-16 mb-4">
            <div class="absolute top-8 left-0 right-0 h-0.5 bg-gray-400"></div>
            <!-- Marcatori -->
            <div class="absolute top-6 left-0 w-0.5 h-4 bg-gray-400"></div>
            <div class="absolute top-6 left-1/2 w-0.5 h-4 bg-gray-400"></div>
            <div class="absolute top-6 right-0 w-0.5 h-4 bg-gray-400"></div>
            <!-- Etichette -->
            <span class="absolute top-0 left-0 transform -translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">0</span>
            <span class="absolute top-0 left-1/2 transform -translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">1</span>
            <span class="absolute top-0 right-0 transform translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">2</span>
            <!-- Sotto 1: 4/4 -->
            <span class="absolute top-12 left-1/2 transform -translate-x-1/2"><%= render 'shared/frazione', num: 4, den: 4 %></span>
            <!-- Divisioni tra 0 e 1 -->
            <% 3.times do |i| %>
              <div class="absolute top-7 w-0.5 h-2 bg-gray-300" style="left: <%= (i + 1) * 12.5 %>%"></div>
            <% end %>
            <!-- Input per posizionare 2/4 -->
            <div class="absolute top-12" style="left: 25%">
              <%= render 'shared/frazione', num: nil, den: nil, num_answer: "2", den_answer: "4" %>
            </div>
          </div>
        </div>

        <!-- Immagine pizza -->
        <div class="flex justify-center items-start">
          <%= image_tag "nvi4_mat/p062/output-1715932804062_680_202_259_183.png", class: "max-w-full h-auto" %>
        </div>
      </div>

      <!-- Box regola frazione propria -->
      <div class="p-3 bg-pink-light rounded-2xl w-fit mt-4">
        <p class="text-gray-700">
          <%= render 'shared/frazione', num: 2, den: 4 %> è una <span class="font-bold">frazione propria</span>: il numeratore è <span class="font-bold">minore</span> del denominatore.
        </p>
      </div>
    </div>

    <!-- Divisore -->
    <div class="border-t-2 border-<%= @pagina.base_color %>-300"></div>

    <!-- Sezione 2: Frazione impropria (6/4) -->
    <div class="py-4">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <p class="text-gray-700 mb-4">
            Lucia mangia <%= render 'shared/frazione', num: 6, den: 4 %>.
          </p>

          <p class="font-bold text-gray-700 mb-3">
            <span class="text-<%= @pagina.base_color %>-500">•</span> Rispondi.
          </p>

          <div class="space-y-3">
            <div class="flex items-center gap-2 flex-wrap">
              <span class="text-gray-700">Quante pizze intere mangia Lucia?</span>
              <input type="text" data-correct-answer="1" class="w-12 border-b-2 border-dotted border-gray-400 text-center font-bold bg-transparent">
            </div>

            <div class="flex items-center gap-2 flex-wrap">
              <span class="text-gray-700">Quanti pezzi mangia della seconda pizza?</span>
              <input type="text" data-correct-answer="2" class="w-12 border-b-2 border-dotted border-gray-400 text-center font-bold bg-transparent">
            </div>

            <p class="text-gray-700">
              La frazione <%= render 'shared/frazione', num: 6, den: 4 %> è <span class="font-bold">maggiore dell'intero</span>.
            </p>
          </div>

          <p class="font-bold text-gray-700 mt-4 mb-3">
            <span class="text-<%= @pagina.base_color %>-500">•</span> Sistema sulla linea la frazione <%= render 'shared/frazione', num: 6, den: 4 %>.
          </p>

          <!-- Linea dei numeri 0-2 -->
          <div class="relative w-full max-w-md h-16 mb-4">
            <div class="absolute top-8 left-0 right-0 h-0.5 bg-gray-400"></div>
            <!-- Marcatori -->
            <div class="absolute top-6 left-0 w-0.5 h-4 bg-gray-400"></div>
            <div class="absolute top-6 left-1/2 w-0.5 h-4 bg-gray-400"></div>
            <div class="absolute top-6 right-0 w-0.5 h-4 bg-gray-400"></div>
            <!-- Etichette -->
            <span class="absolute top-0 left-0 transform -translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">0</span>
            <span class="absolute top-0 left-1/2 transform -translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">1</span>
            <span class="absolute top-0 right-0 transform translate-x-1/2 text-<%= @pagina.base_color %>-600 font-bold">2</span>
            <!-- Sotto 1: 4/4 -->
            <span class="absolute top-12 left-1/2 transform -translate-x-1/2"><%= render 'shared/frazione', num: 4, den: 4 %></span>
            <!-- Divisioni tra 0 e 1 -->
            <% 3.times do |i| %>
              <div class="absolute top-7 w-0.5 h-2 bg-gray-300" style="left: <%= (i + 1) * 12.5 %>%"></div>
            <% end %>
            <!-- Divisioni tra 1 e 2 -->
            <% 3.times do |i| %>
              <div class="absolute top-7 w-0.5 h-2 bg-gray-300" style="left: <%= 50 + (i + 1) * 12.5 %>%"></div>
            <% end %>
            <!-- Input per posizionare 6/4 -->
            <div class="absolute top-12" style="left: 75%">
              <%= render 'shared/frazione', num: nil, den: nil, num_answer: "6", den_answer: "4" %>
            </div>
          </div>
        </div>

        <!-- Immagine pizze -->
        <div class="flex justify-center items-start">
          <%= image_tag "nvi4_mat/p062/output-1715932804062_678_629_264_361.png", class: "max-w-full h-auto" %>
        </div>
      </div>

      <!-- Box regola frazione impropria -->
      <div class="p-3 bg-pink-light rounded-2xl w-fit mt-4">
        <p class="text-gray-700">
          <%= render 'shared/frazione', num: 6, den: 4 %> è una <span class="font-bold">frazione impropria</span>: il numeratore è <span class="font-bold">maggiore</span> del denominatore.
        </p>
      </div>
    </div>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
