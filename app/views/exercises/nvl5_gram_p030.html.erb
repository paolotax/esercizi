<div class="max-w-6xl mx-auto p-3 md:p-8 bg-gradient-to-br from-amber-50 via-white to-purple-50 rounded-3xl shadow-xl" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Introduction -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-amber-200 shadow-2xl p-4 md:p-6">
    <p class="text-base md:text-lg text-gray-700 mb-3">
      <em class="text-amber-700">Litigare / discutere, compagno / coetaneo, testardo / caparbio</em> sono sinonimi,
      ma non sono perfettamente intercambiabili: hanno <strong>sfumature diverse</strong> e si usano in contesti differenti.
    </p>
    <p class="text-base md:text-lg text-gray-700 mb-2">
      Le parole del <strong class="text-blue-700">registro informale</strong> (o familiare) si usano quando parliamo con persone che conosciamo.
    </p>
    <p class="text-base md:text-lg text-gray-700">
      Le parole del <strong class="text-purple-700">registro formale</strong> (o ricercato) si usano soprattutto quando si scrive o quando parliamo con persone con cui non abbiamo confidenza.
    </p>
  </div>

  <!-- Exercise 1: Identify informal and formal -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-blue-200 shadow-2xl p-4 md:p-6" data-controller="exercise-group">
    <div class="mb-4 inline-block bg-gradient-to-r from-amber-500 to-purple-500 text-white px-3 md:px-6 py-2 rounded-full text-xs md:text-sm font-bold shadow-lg">
      1 IMPARARE TUTTI
    </div>
    <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-3">
      Per ogni coppia di sinonimi indica quello informale (I) e quello formale (F).
    </h2>

    <% word_pairs = [
      { word1: "gota", register1: "F", word2: "guancia", register2: "I" },
      { word1: "vano", register1: "F", word2: "inutile", register2: "I" },
      { word1: "criticare", register1: "I", word2: "biasimare", register2: "F" },
      { word1: "esile", register1: "F", word2: "magro", register2: "I" },
      { word1: "ricordare", register1: "I", word2: "rammentare", register2: "F" },
      { word1: "carino", register1: "I", word2: "aggraziato", register2: "F" },
      { word1: "fievole", register1: "F", word2: "debole", register2: "I" },
      { word1: "frastuono", register1: "F", word2: "rumore", register2: "I" }
    ] %>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-base md:text-lg">
      <% word_pairs.each_with_index do |pair, index| %>
        <div class="bg-blue-50 border-2 border-blue-200 rounded-xl px-3 py-3">
          <div class="flex items-center gap-3 mb-2">
            <span class="text-gray-700">•</span>
            <span class="font-semibold text-blue-700 flex-1"><%= pair[:word1] %></span>
            <label class="flex items-center gap-1">
              <input type="checkbox" name="pair<%= index %>_word1" value="I" <%= 'checked' if pair[:register1] == 'I' %> data-correct-answer="<%= pair[:register1] %>" class="w-4 h-4">
              <span class="text-xs">I</span>
            </label>
            <label class="flex items-center gap-1">
              <input type="checkbox" name="pair<%= index %>_word1" value="F" <%= 'checked' if pair[:register1] == 'F' %> data-correct-answer="<%= pair[:register1] %>" class="w-4 h-4">
              <span class="text-xs">F</span>
            </label>
          </div>
          <div class="flex items-center gap-3">
            <span class="text-gray-700">•</span>
            <span class="font-semibold text-blue-700 flex-1"><%= pair[:word2] %></span>
            <label class="flex items-center gap-1">
              <input type="checkbox" name="pair<%= index %>_word2" value="I" <%= 'checked' if pair[:register2] == 'I' %> data-correct-answer="<%= pair[:register2] %>" class="w-4 h-4">
              <span class="text-xs">I</span>
            </label>
            <label class="flex items-center gap-1">
              <input type="checkbox" name="pair<%= index %>_word2" value="F" <%= 'checked' if pair[:register2] == 'F' %> data-correct-answer="<%= pair[:register2] %>" class="w-4 h-4">
              <span class="text-xs">F</span>
            </label>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Writing exercise -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-purple-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-purple-700 mb-3">
      <span class="font-bold">2</span> Scrivi le frasi sul quaderno: sostituisci le parole o le espressioni sottolineate con sinonimi del registro informale.
    </h2>

    <% formal_sentences = [
      "Gradisci un gelato? Il mio è squisito!",
      "Mi rincresce per ciò che è accaduto ieri.",
      "– Questa minestra è insipida – bofonchiava il nonno.",
      "Era un bambino gracile, con un volto sorridente.",
      "I compiti di matematica erano davvero complessi."
    ] %>

    <div class="space-y-2 text-base md:text-lg">
      <% formal_sentences.each do |sentence| %>
        <div class="bg-purple-50 border-2 border-purple-200 rounded-xl px-3 py-3">
          <p class="text-gray-700"><%= sentence %></p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Parole in scala section -->
  <div class="mb-6 bg-gradient-to-r from-teal-100 to-blue-100 rounded-2xl border-4 border-teal-300 shadow-2xl p-4 md:p-6">
    <h2 class="text-xl md:text-2xl font-bold text-teal-700 mb-3">
      Parole… in scala!
    </h2>

    <div class="bg-white rounded-xl p-4 mb-4">
      <p class="text-base md:text-lg text-gray-700 mb-2">
        Osserva il disegno, leggi e rispondi.
      </p>
      <p class="text-base md:text-lg text-gray-700 mb-3 italic">
        Pic è <strong class="text-red-600">minuscolo</strong>.
        Zic è <strong class="text-orange-600">piccolo</strong>.
        Fop è <strong class="text-yellow-600">normale</strong>.
        Dap è <strong class="text-green-600">grande</strong>.
        Tuc è <strong class="text-blue-600">enorme</strong>.
      </p>

      <div class="space-y-2 text-base md:text-lg">
        <label class="flex items-start gap-2">
          <span class="text-gray-700">- Minuscolo, piccolo, normale, grande, enorme sono sinonimi?</span>
          <div class="flex gap-2">
            <label class="flex items-center gap-1">
              <input type="checkbox" value="Sì" class="w-4 h-4">
              <span>Sì</span>
            </label>
            <label class="flex items-center gap-1">
              <input type="checkbox" value="No" data-correct-answer="No" class="w-4 h-4">
              <span>No</span>
            </label>
          </div>
        </label>

        <label class="flex items-start gap-2">
          <span class="text-gray-700">- Minuscolo, piccolo, normale, grande, enorme cambiano un po' per volta?</span>
          <div class="flex gap-2">
            <label class="flex items-center gap-1">
              <input type="checkbox" value="Sì" data-correct-answer="Sì" class="w-4 h-4">
              <span>Sì</span>
            </label>
            <label class="flex items-center gap-1">
              <input type="checkbox" value="No" class="w-4 h-4">
              <span>No</span>
            </label>
          </div>
        </label>

        <label class="flex items-start gap-2">
          <span class="text-gray-700">- Enorme è il contrario di minuscolo?</span>
          <div class="flex gap-2">
            <label class="flex items-center gap-1">
              <input type="checkbox" value="Sì" data-correct-answer="Sì" class="w-4 h-4">
              <span>Sì</span>
            </label>
            <label class="flex items-center gap-1">
              <input type="checkbox" value="No" class="w-4 h-4">
              <span>No</span>
            </label>
          </div>
        </label>
      </div>
    </div>
  </div>

  <!-- Exercise 1: Check if scales are correct -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-green-200 shadow-2xl p-4 md:p-6" data-controller="exercise-group">
    <div class="mb-4 inline-block bg-gradient-to-r from-teal-500 to-green-500 text-white px-3 md:px-6 py-2 rounded-full text-xs md:text-sm font-bold shadow-lg">
      1 IMPARARE TUTTI
    </div>
    <h2 class="text-lg md:text-xl font-bold text-green-700 mb-3">
      Indica con X se le seguenti serie di parole rispettano la "scala" di significato.
    </h2>

    <% scales = [
      { text: "Un film: brutto – bruttino – gradevole – bello – stupendo", correct: "Sì" },
      { text: "Un viso: allegro – sorridente – triste – serio – entusiasta", correct: "No" },
      { text: "Una finestra: sbarrata – chiusa – socchiusa – aperta – spalancata", correct: "Sì" },
      { text: "Un uomo: ossuto – magro – snello – paffuto – grasso", correct: "Sì" }
    ] %>

    <div class="space-y-3 text-base md:text-lg">
      <% scales.each_with_index do |scale, index| %>
        <div class="bg-green-50 border-2 border-green-200 rounded-xl px-3 py-3">
          <p class="text-gray-700 mb-2"><%= scale[:text] %></p>
          <div class="flex gap-4">
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="radio" name="scale<%= index %>" value="Sì" data-correct-answer="<%= scale[:correct] %>" class="w-5 h-5">
              <span>Sì</span>
            </label>
            <label class="flex items-center gap-2 cursor-pointer">
              <input type="radio" name="scale<%= index %>" value="No" data-correct-answer="<%= scale[:correct] %>" class="w-5 h-5">
              <span>No</span>
            </label>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Write words in scale -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-orange-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-orange-700 mb-3">
      <span class="font-bold">2</span> Leggi le parole, poi scrivile "in scala". Parti dalla parola sottolineata.
    </h2>

    <% word_scales = [
      {
        words: "pessimo - buono - ottimo - sufficiente - discreto",
        scale: ["pessimo", "sufficiente", "discreto", "buono", "ottimo"]
      },
      {
        words: "bollente - ghiacciato - caldo - freddo - tiepido",
        scale: ["ghiacciato", "freddo", "tiepido", "caldo", "bollente"]
      },
      {
        words: "secco - umido - asciutto - fradicio - bagnato",
        scale: ["secco", "asciutto", "umido", "bagnato", "fradicio"]
      }
    ] %>

    <div class="space-y-6 text-base md:text-lg">
      <% word_scales.each_with_index do |item, set_index| %>
        <div class="bg-orange-50 border-2 border-orange-200 rounded-xl px-3 py-4">
          <p class="text-gray-700 font-semibold mb-3"><%= item[:words] %></p>
          <div class="space-y-2">
            <% item[:scale].each_with_index do |word, index| %>
              <div class="flex items-center gap-2">
                <span class="font-bold text-orange-700 w-6"><%= index + 1 %></span>
                <input type="text"
                       class="flex-1 border-b-2 border-orange-500 bg-white rounded px-3 py-1 focus:outline-none focus:border-orange-700"
                       data-fill-blanks-target="input"
                       data-answer="<%= word %>"
                       data-correct-answer="<%= word %>"
                       placeholder="........................"
                       maxlength="20">
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "141", pagina_path("nvl5_gram_p141"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise Controls -->
  <div class="mt-6">
    <%= render 'shared/exercise_controls', color: 'amber', current_page: 'nvl5_gram_p030' %>
  </div>
</div>
