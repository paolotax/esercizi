<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 text-black dark:text-white p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

  <!-- Introduction -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8">
    <p class="text-base md:text-lg text-gray-700 dark:text-gray-300 mb-3 font-semibold">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Gli omonimi hanno più significati e valori grammaticali diversi.
    </p>
    <p class="text-base md:text-lg text-gray-700 dark:text-gray-300 mb-1">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Manca un'<strong>ora</strong> <span class="text-sm text-gray-500 dark:text-gray-400">(nome)</span> a mezzanotte.
    </p>
    <p class="text-base md:text-lg text-gray-700 dark:text-gray-300">
      <span class="text-<%= @pagina.base_color %>-500">•</span> <strong>Ora</strong> <span class="text-sm text-gray-500 dark:text-gray-400">(avverbio)</span> facciamo merenda.
    </p>
  </div>

  <!-- Exercise 1: Complete with indicated word -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-gray-800 dark:text-gray-200 mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Completa le frasi con la parola indicata, poi scrivi il corrispondente valore grammaticale.
    </h2>

    <% homonym_groups = [
      {
        word: "PIANO",
        sentences: [
          { text: "Prendi un piatto piano.", answer: "", filled: true },
          { text: "Abitiamo allo stesso", answer: "piano" }
        ],
        values: ["aggettivo", "nome"],
        example: true
      },
      {
        word: "PORTA",
        sentences: [
          { text: "Non ho sbattuto io la", answer: "porta" },
          { text: "", answer: "Porta", suffix: "questo pacco alla nonna, per favore." }
        ],
        values: ["nome", "verbo"]
      },
      {
        word: "FIERA",
        sentences: [
          { text: "Sono una maestra", answer: "fiera", suffix: "dei propri alunni." },
          { text: "Sabato inizierà la", answer: "fiera", suffix: "dell'artigianato." }
        ],
        values: ["aggettivo", "nome"]
      }
    ] %>

    <div class="space-y-5 text-gray-800 dark:text-gray-200">
      <% homonym_groups.each_with_index do |group, group_index| %>
        <div class="rounded-lg p-3">
          <p class="text-lg font-bold text-<%= @pagina.base_color %>-700 dark:text-<%= @pagina.base_color %>-400 mb-3"><%= group[:word] %></p>
          <div class="space-y-2 mb-3">
            <% group[:sentences].each_with_index do |sentence, sent_index| %>
              <div class="flex flex-wrap items-center gap-2 text-base md:text-lg">
                <span class="text-<%= @pagina.base_color %>-500">•</span>
                <% if sentence[:text].present? %>
                  <span class="text-gray-700 dark:text-gray-300"><%= sentence[:text] %></span>
                <% end %>
                <% if sentence[:filled] %>
                  <span class="text-gray-700 dark:text-gray-300">.</span>
                <% else %>
                  <input type="text"
                         class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                         data-fill-blanks-target="input"
                         data-answer="<%= sentence[:answer] %>"
                         data-correct-answer="<%= sentence[:answer] %>"
                         maxlength="20">
                <% end %>
                <% if sentence[:suffix] %>
                  <span class="text-gray-700 dark:text-gray-300"><%= sentence[:suffix] %></span>
                <% end %>
              </div>
            <% end %>
          </div>
          <div class="flex flex-wrap items-center gap-2 text-base md:text-lg">
            <span class="text-gray-700 dark:text-gray-300">=</span>
            <% if group[:example] %>
              <span class="text-gray-700 dark:text-gray-300"><%= group[:values][0] %></span>
              <span class="text-gray-700 dark:text-gray-300">/</span>
              <input type="text"
                     class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                     data-fill-blanks-target="input"
                     data-answer="<%= group[:values][1] %>"
                     data-correct-answer="<%= group[:values][1] %>"
                     maxlength="20">
            <% else %>
              <input type="text"
                     class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                     data-fill-blanks-target="input"
                     data-answer="<%= group[:values][0] %>"
                     data-correct-answer="<%= group[:values][0] %>"
                     maxlength="20">
              <span class="text-gray-700 dark:text-gray-300">/</span>
              <input type="text"
                     class="w-28 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-transparent dark:text-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                     data-fill-blanks-target="input"
                     data-answer="<%= group[:values][1] %>"
                     data-correct-answer="<%= group[:values][1] %>"
                     maxlength="20">
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Write sentences -->
  <div class="bg-white dark:bg-gray-800 rounded-lg p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold text-gray-800 dark:text-gray-200 mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Per ogni parola scrivi una frase con ciascuno dei valori grammaticali indicati.
    </h2>

    <% sentence_exercises = [
      { word: "piatto", values: "nome: ................................ / aggettivo: ................................" },
      { word: "parte", values: "nome: ................................ / verbo: ................................" },
      { word: "amare", values: "aggettivo: ................................ / verbo: ................................" }
    ] %>

    <div class="space-y-3 text-base md:text-lg text-gray-800 dark:text-gray-200">
      <% sentence_exercises.each do |exercise| %>
        <div class="rounded-lg p-3">
          <p class="text-gray-700 dark:text-gray-300">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <strong class="text-<%= @pagina.base_color %>-700 dark:text-<%= @pagina.base_color %>-400"><%= exercise[:word] %></strong> → <%= exercise[:values] %>
          </p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
  </div><!-- chiude space-y-6 -->
</div>
