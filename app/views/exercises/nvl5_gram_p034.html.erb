<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Introduction -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <p class="text-base md:text-lg text-gray-700 mb-3">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Ogni disciplina o attività (geografia, informatica, sport…) usa un <strong>linguaggio specifico</strong> chiamato <strong>linguaggio settoriale</strong> o <strong>tecnico</strong>.
    </p>
    <div class="bg-<%= @pagina.base_color %>-50 rounded-lg p-3">
      <p class="text-base md:text-lg text-gray-700 mb-2">
        <span class="text-<%= @pagina.base_color %>-500">•</span> Il linguaggio settoriale definisce con parole precise concetti di uso quotidiano:
      </p>
      <p class="text-base text-gray-700">
        <span class="text-<%= @pagina.base_color %>-500">•</span> linguaggio della geografia → <strong>rilievi collinari | fauna | flora</strong>
      </p>
      <p class="text-base text-gray-700">
        <span class="text-<%= @pagina.base_color %>-500">•</span> espressioni comuni → <strong>colline | animali | piante</strong>
      </p>
    </div>
  </div>

  <!-- Exercise 1: Identify sector language words -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">1</span>
      Cerchia le parole che non conosci o che pensi possano far parte di quel linguaggio.
    </h2>

    <% sector_sentences = [
      { title: "Linguaggio dell'informatica", text: "Se non riusciamo a eliminare il virus, dovremo resettare la memoria." },
      { title: "Linguaggio della medicina", text: "Per questa tendinite l'unico rimedio sono antinfiammatori e riposo." },
      { title: "Linguaggio dell'ecologia", text: "I funghi sono esseri eterotrofi importanti nelle reti alimentari di ogni habitat." },
      { title: "Linguaggio della politica", text: "In Senato si vota la fiducia. Ci sono dubbi sulla tenuta della maggioranza di governo." }
    ] %>

    <div class="space-y-4">
      <% sector_sentences.each do |sentence| %>
        <div class="rounded-lg p-3">
          <p class="text-sm md:text-base font-bold text-<%= @pagina.base_color %>-700 mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= sentence[:title] %></p>
          <p class="text-base md:text-lg text-gray-700"><%= sentence[:text] %></p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 2: Match language sectors -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">2</span>
      Scrivi a quale linguaggio settoriale appartiene ogni gruppo di parole.
    </h2>

    <% word_groups = [
      { words: "centravanti • fuorigioco • cross", answer: "dello sport" },
      { words: "stonatura • armonia • composizione", answer: "della musica" },
      { words: "elemento • reazione • composto", answer: "della chimica" }
    ] %>

    <div class="space-y-3 text-base md:text-lg">
      <% word_groups.each do |group| %>
        <div class="rounded-lg p-3 flex items-center justify-between gap-4">
          <div class="flex items-center gap-4">
            <span class="text-<%= @pagina.base_color %>-500">•</span>
            <span class="text-gray-700"><%= group[:words] %></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
          </div>
          <input type="text"
                 class="w-40 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= group[:answer] %>"
                 data-correct-answer="<%= group[:answer] %>"
                 maxlength="40">
        </div>
      <% end %>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "142", pagina_path("nvl5_gram_p142"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise 3: Match technical words with common synonyms -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">3</span>
      Collega ogni parola di un linguaggio tecnico al suo sinonimo nel linguaggio comune.
    </h2>

    <div class="bg-<%= @pagina.base_color %>-50 rounded-lg p-3 mb-3">
      <p class="text-base md:text-lg text-gray-700 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> Linguaggio tecnico:</p>
      <p class="text-base md:text-lg text-<%= @pagina.base_color %>-700">addome | lipidi | sanzione | calzatura | igienizzazione | pedana</p>
    </div>

    <div class="bg-gray-50 rounded-lg p-3">
      <p class="text-base md:text-lg text-gray-700 font-semibold mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> Linguaggio comune:</p>
      <p class="text-base md:text-lg text-gray-700">pulizia | pancia | scarpa | grassi | base | multa</p>
    </div>
  </div>

  <!-- Exercise 4: Complete table with meanings -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">4</span>
      Completa la tabella scrivendo con parole tue il significato di ciascun termine tecnico.
    </h2>

    <% technical_terms = [
      { term: "encefalo", sector: "della medicina e della biologia" },
      { term: "trave", sector: "dell'architettura" },
      { term: "inquadratura", sector: "del cinema e della fotografia" },
      { term: "coreografia", sector: "della danza" },
      { term: "acquerello", sector: "dell'arte" }
    ] %>

    <div class="overflow-x-auto">
      <table class="w-full rounded-lg overflow-hidden">
        <thead>
          <tr class="bg-<%= @pagina.base_color %>-100">
            <th class="px-3 py-2 text-left text-sm md:text-base font-bold text-gray-800">Termine tecnico</th>
            <th class="px-3 py-2 text-left text-sm md:text-base font-bold text-gray-800">Linguaggio...</th>
            <th class="px-3 py-2 text-left text-sm md:text-base font-bold text-gray-800">Significato</th>
          </tr>
        </thead>
        <tbody class="bg-white">
          <% technical_terms.each do |item| %>
            <tr>
              <td class="border-b border-<%= @pagina.base_color %>-200 px-3 py-3 font-semibold text-<%= @pagina.base_color %>-700"><%= item[:term] %></td>
              <td class="border-b border-<%= @pagina.base_color %>-200 px-3 py-3 text-gray-700"><%= item[:sector] %></td>
              <td class="border-b border-<%= @pagina.base_color %>-200 px-3 py-3 text-gray-500">.......................................</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Exercise 5: Write geography terms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">5</span>
      Scrivi alcuni termini specifici del linguaggio della geografia.
    </h2>

    <div class="rounded-lg p-3">
      <p class="text-base md:text-lg text-gray-700">
        <span class="text-<%= @pagina.base_color %>-500">•</span> vetta, <input type="text"
                      class="border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 py-1 focus:outline-none focus:border-<%= @pagina.base_color %>-700 w-full max-w-2xl"
                      maxlength="100">
      </p>
    </div>
  </div>

  <!-- Game section -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">🎮</span>
      Gioco
    </h2>
    <p class="text-base md:text-lg text-gray-700">
      <span class="text-<%= @pagina.base_color %>-500">•</span> Ritagliate da un giornale gli articoli indicati dall'insegnante e dividetevi in piccoli gruppi. Sottolineate le parole tecniche e confrontatevi: a quale linguaggio appartengono?
    </p>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
</div>
