<div class="max-w-6xl mx-auto bg-white dark:bg-gray-900 p-3 md:p-6 transition-colors duration-300"
     data-controller="exercise-checker text-toggle font-controls fraction-coloring word-highlighter"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">

    <!-- Sezione teoria: cerchi -->
    <div class="py-4">
      <p class="font-bold text-gray-700 dark:text-gray-200 mb-4">
        <span class="text-<%= @pagina.base_color %>-500">•</span> Scrivi la frazione che rappresenta la parte colorata di ogni cerchio, poi rispondi alle domande.
      </p>

      <!-- 4 cerchi -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
        <!-- Cerchio 1: 1/2 -->
        <div class="flex flex-col items-center gap-2">
          <div class="relative w-24 h-24">
            <svg viewBox="0 0 100 100" class="w-full h-full">
              <path d="M50,50 L50,5 A45,45 0 0,1 50,95 Z" fill="#fdba74" stroke="#9ca3af" stroke-width="1"/>
              <path d="M50,50 L50,95 A45,45 0 0,1 50,5 Z" fill="white" stroke="#9ca3af" stroke-width="1"/>
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9ca3af" stroke-width="2"/>
            </svg>
          </div>
          <%= render 'shared/frazione', num: nil, den: nil, num_answer: "1", den_answer: "2" %>
        </div>

        <!-- Cerchio 2: 2/4 -->
        <div class="flex flex-col items-center gap-2">
          <div class="relative w-24 h-24">
            <svg viewBox="0 0 100 100" class="w-full h-full">
              <% 4.times do |i| %>
                <path d="M50,50 L<%= 50 + 45 * Math.cos((i * 90 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin((i * 90 - 90) * Math::PI / 180) %> A45,45 0 0,1 <%= 50 + 45 * Math.cos(((i + 1) * 90 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin(((i + 1) * 90 - 90) * Math::PI / 180) %> Z"
                      fill="<%= i < 2 ? '#fdba74' : 'white' %>"
                      stroke="#9ca3af"
                      stroke-width="1"/>
              <% end %>
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9ca3af" stroke-width="2"/>
            </svg>
          </div>
          <%= render 'shared/frazione', num: nil, den: nil, num_answer: "2", den_answer: "4" %>
        </div>

        <!-- Cerchio 3: 3/6 -->
        <div class="flex flex-col items-center gap-2">
          <div class="relative w-24 h-24">
            <svg viewBox="0 0 100 100" class="w-full h-full">
              <% 6.times do |i| %>
                <path d="M50,50 L<%= 50 + 45 * Math.cos((i * 60 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin((i * 60 - 90) * Math::PI / 180) %> A45,45 0 0,1 <%= 50 + 45 * Math.cos(((i + 1) * 60 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin(((i + 1) * 60 - 90) * Math::PI / 180) %> Z"
                      fill="<%= i < 3 ? '#fdba74' : 'white' %>"
                      stroke="#9ca3af"
                      stroke-width="1"/>
              <% end %>
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9ca3af" stroke-width="2"/>
            </svg>
          </div>
          <%= render 'shared/frazione', num: nil, den: nil, num_answer: "3", den_answer: "6" %>
        </div>

        <!-- Cerchio 4: 4/8 -->
        <div class="flex flex-col items-center gap-2">
          <div class="relative w-24 h-24">
            <svg viewBox="0 0 100 100" class="w-full h-full">
              <% 8.times do |i| %>
                <path d="M50,50 L<%= 50 + 45 * Math.cos((i * 45 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin((i * 45 - 90) * Math::PI / 180) %> A45,45 0 0,1 <%= 50 + 45 * Math.cos(((i + 1) * 45 - 90) * Math::PI / 180) %>,<%= 50 + 45 * Math.sin(((i + 1) * 45 - 90) * Math::PI / 180) %> Z"
                      fill="<%= i < 4 ? '#fdba74' : 'white' %>"
                      stroke="#9ca3af"
                      stroke-width="1"/>
              <% end %>
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9ca3af" stroke-width="2"/>
            </svg>
          </div>
          <%= render 'shared/frazione', num: nil, den: nil, num_answer: "4", den_answer: "8" %>
        </div>
      </div>

      <!-- Domande -->
      <div class="space-y-3">
        <div class="flex items-center gap-4 flex-wrap">
          <span class="text-gray-700 dark:text-gray-200">La parte colorata corrisponde sempre a <%= render 'shared/frazione', num: 1, den: 2 %> della figura?</span>
          <div class="flex gap-2">
            <label class="relative flex items-center justify-center w-10 h-8 bg-white dark:bg-gray-700 dark:text-gray-200 border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
              <input type="radio" name="metafigura" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="true">
              <span>Sì</span>
            </label>
            <label class="relative flex items-center justify-center w-10 h-8 bg-white dark:bg-gray-700 dark:text-gray-200 border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
              <input type="radio" name="metafigura" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="false">
              <span>No</span>
            </label>
          </div>
        </div>

        <div class="flex items-center gap-4 flex-wrap">
          <span class="text-gray-700 dark:text-gray-200">Le frazioni indicano la stessa parte dell'intero?</span>
          <div class="flex gap-2">
            <label class="relative flex items-center justify-center w-10 h-8 bg-white dark:bg-gray-700 dark:text-gray-200 border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
              <input type="radio" name="stessaparte" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="true">
              <span>Sì</span>
            </label>
            <label class="relative flex items-center justify-center w-10 h-8 bg-white dark:bg-gray-700 dark:text-gray-200 border-2 border-<%= @pagina.base_color %>-500 rounded cursor-pointer hover:bg-<%= @pagina.base_color %>-100 has-checked:bg-pink-300">
              <input type="radio" name="stessaparte" class="absolute inset-0 opacity-0 cursor-pointer" data-correct-answer="false">
              <span>No</span>
            </label>
          </div>
        </div>

        <p class="text-gray-700 dark:text-gray-200">
          Le frazioni <%= render 'shared/frazione', num: 1, den: 2 %>, <%= render 'shared/frazione', num: 2, den: 4 %>, <%= render 'shared/frazione', num: 3, den: 6 %> e <%= render 'shared/frazione', num: 4, den: 8 %> rappresentano la stessa parte dell'intero: sono <span class="font-bold">frazioni equivalenti</span>.
        </p>
      </div>
    </div>

    <!-- Box regola -->
    <div class="p-3 bg-pink-light dark:bg-pink-900/40 rounded-2xl w-fit">
      <p class="text-gray-700 dark:text-gray-200">
        Le frazioni che rappresentano la stessa parte dell'intero si chiamano <span class="font-bold">frazioni equivalenti</span>.
      </p>
    </div>

    <!-- Come ottenere frazioni equivalenti -->
    <div class="py-4 border-t border-gray-200 dark:border-gray-700">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <p class="text-gray-700 dark:text-gray-200 mb-4">
            Per ottenere frazioni equivalenti <span class="font-bold">moltiplica</span> o <span class="font-bold">dividi</span> il <span class="font-bold">numeratore e il denominatore per uno stesso numero</span> diverso da zero.
          </p>
        </div>

        <div class="space-y-4">
          <!-- Moltiplicazione -->
          <div class="flex items-center gap-2 flex-wrap">
            <%= render 'shared/frazione', num: 1, den: 2 %>
            <span class="text-blue-600 font-bold">× 2<br>× 2</span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: 2, den: 4 %>

            <span class="mx-4"></span>

            <%= render 'shared/frazione', num: 2, den: 4 %>
            <span class="text-blue-600 font-bold">× 2<br>× 2</span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: 4, den: 8 %>

            <span class="mx-4"></span>

            <%= render 'shared/frazione', num: 1, den: 2 %>
            <span class="text-blue-600 font-bold">×<input type="text" data-correct-answer="3" class="w-8 border-b border-gray-400 text-center bg-transparent"><br>×<input type="text" data-correct-answer="3" class="w-8 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: 3, den: 6 %>
          </div>

          <!-- Divisione -->
          <div class="flex items-center gap-2 flex-wrap">
            <%= render 'shared/frazione', num: 5, den: 45 %>
            <span class="text-orange-600 font-bold">: 5<br>: 5</span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "1", den_answer: "9" %>

            <span class="mx-4"></span>

            <%= render 'shared/frazione', num: 6, den: 21 %>
            <span class="text-orange-600 font-bold">: 3<br>: 3</span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "2", den_answer: "7" %>

            <span class="mx-4"></span>

            <%= render 'shared/frazione', num: 14, den: 35 %>
            <span class="text-orange-600 font-bold">:<input type="text" data-correct-answer="7" class="w-8 border-b border-gray-400 text-center bg-transparent"><br>:<input type="text" data-correct-answer="7" class="w-8 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "2", den_answer: "5" %>
          </div>
        </div>
      </div>
    </div>

    <!-- ESERCIZI -->
    <div class="p-4 md:p-6 bg-orange-100 dark:bg-orange-900/30 rounded-2xl">

      <!-- Esercizio 1 -->
      <div class="p-4 bg-white dark:bg-gray-800 rounded-2xl -mx-4 md:-mx-6 -mt-4 md:-mt-6 mb-6 border-3 border-blue-500">
        <p class="font-bold text-gray-700 dark:text-gray-200 mb-4">
          <%= imparare_tutti_badge(1) %>
          Colora la frazione indicata, poi cerchia con lo stesso colore le frazioni equivalenti.
        </p>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
          <!-- 1/2 -->
          <div class="flex flex-col items-center gap-2">
            <div class="w-20 h-16 border-2 border-gray-400 grid grid-cols-2">
              <div class="border-r border-gray-400 cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
              <div class="cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
            </div>
            <%= render 'shared/frazione', num: 1, den: 2 %>
          </div>

          <!-- 3/4 -->
          <div class="flex flex-col items-center gap-2">
            <div class="w-20 h-16 border-2 border-gray-400 grid grid-cols-4">
              <% 4.times do |i| %>
                <div class="<%= i < 3 ? 'border-r border-gray-400' : '' %> cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
              <% end %>
            </div>
            <%= render 'shared/frazione', num: 3, den: 4 %>
          </div>

          <!-- 4/8 -->
          <div class="flex flex-col items-center gap-2">
            <div class="w-20 h-16 border-2 border-gray-400 grid grid-cols-4 grid-rows-2">
              <% 8.times do |i| %>
                <div class="border border-gray-300 cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
              <% end %>
            </div>
            <%= render 'shared/frazione', num: 4, den: 8 %>
          </div>

          <!-- 6/8 -->
          <div class="flex flex-col items-center gap-2">
            <div class="w-20 h-16 border-2 border-gray-400 grid grid-cols-4 grid-rows-2">
              <% 8.times do |i| %>
                <div class="border border-gray-300 cursor-pointer hover:bg-orange-100 dark:hover:bg-orange-800/50" data-action="click->fraction-coloring#toggleColor"></div>
              <% end %>
            </div>
            <%= render 'shared/frazione', num: 6, den: 8 %>
          </div>
        </div>
      </div>

      <!-- Esercizio 2 -->
      <div>
        <p class="font-bold text-gray-700 dark:text-gray-200 mb-4">
          <%= numero_esercizio_badge(2) %>
          Trova una frazione equivalente a ogni frazione data.
        </p>

        <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
          <!-- 6/10 -->
          <div class="flex items-center gap-2">
            <%= render 'shared/frazione', num: 6, den: 10 %>
            <span class="text-orange-600 font-bold">: 2<br>: 2</span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "3", den_answer: "5" %>
          </div>

          <!-- 2/3 -->
          <div class="flex items-center gap-2">
            <%= render 'shared/frazione', num: 2, den: 3 %>
            <span class="text-blue-600 font-bold">×<input type="text" data-correct-answer="2" class="w-6 border-b border-gray-400 text-center bg-transparent"><br>×<input type="text" data-correct-answer="2" class="w-6 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "4", den_answer: "6" %>
          </div>

          <!-- 9/15 -->
          <div class="flex items-center gap-2">
            <%= render 'shared/frazione', num: 9, den: 15 %>
            <span class="text-orange-600 font-bold">:<input type="text" data-correct-answer="3" class="w-6 border-b border-gray-400 text-center bg-transparent"><br>:<input type="text" data-correct-answer="3" class="w-6 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "3", den_answer: "5" %>
          </div>

          <!-- 3/7 -->
          <div class="flex items-center gap-2">
            <%= render 'shared/frazione', num: 3, den: 7 %>
            <span class="text-blue-600 font-bold">×<input type="text" data-correct-answer="2" class="w-6 border-b border-gray-400 text-center bg-transparent"><br>×<input type="text" data-correct-answer="2" class="w-6 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "6", den_answer: "14" %>
          </div>

          <!-- 8/16 -->
          <div class="flex items-center gap-2">
            <%= render 'shared/frazione', num: 8, den: 16 %>
            <span class="text-orange-600 font-bold">:<input type="text" data-correct-answer="8" class="w-6 border-b border-gray-400 text-center bg-transparent"><br>:<input type="text" data-correct-answer="8" class="w-6 border-b border-gray-400 text-center bg-transparent"></span>
            <span class="text-<%= @pagina.base_color %>-500">→</span>
            <%= render 'shared/frazione', num: nil, den: nil, num_answer: "1", den_answer: "2" %>
          </div>
        </div>
      </div>
    </div>

    <%= render 'shared/quaderno_link', pagine: [203, 208] %>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
