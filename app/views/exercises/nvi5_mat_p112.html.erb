<div class="max-w-6xl mx-auto bg-white p-3 md:p-6"
     data-controller="exercise-checker text-toggle font-controls"
     data-text-toggle-target="content"
     data-font-controls-target="content">

  <%= render 'shared/page_header', pagina: @pagina %>

  <div class="space-y-6">
    <!-- Introduzione -->
    <div class="p-4">
      <p class="text-<%= @pagina.base_color %>-600 text-center font-bold">
        Per disegnare, misurare e confrontare figure ed elementi geometrici si possono usare tanti strumenti. Scopriamone insieme alcuni.
      </p>
    </div>

    <!-- Strumenti 1 e 2 affiancati -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- 1. Righello non graduato -->
      <div>
        <div class="p-3 bg-yellow-light rounded-full mb-4 flex items-start gap-2">
          <span class="inline-flex items-center justify-center w-6 h-6 bg-<%= @pagina.base_color %>-500 text-white rounded-full font-bold shrink-0 -ml-3">1</span>
          <p class="text-gray-700">
            Il <strong class="text-<%= @pagina.base_color %>-600">righello non graduato</strong> serve per tracciare una retta e per verificare se due o più punti sono allineati.
          </p>
        </div>
        <div class="flex justify-center">
          <%= image_tag "nvi5_mat/p112/p112_02.jpg", class: "max-w-full", alt: "Righello non graduato" %>
        </div>
      </div>

      <!-- 2. Riga graduata -->
      <div>
        <div class="p-3 bg-yellow-light rounded-full mb-4 flex items-start gap-2">
          <span class="inline-flex items-center justify-center w-6 h-6 bg-<%= @pagina.base_color %>-500 text-white rounded-full font-bold shrink-0 -ml-3">2</span>
          <p class="text-gray-700">
            La <strong class="text-<%= @pagina.base_color %>-600">riga graduata</strong> serve per tracciare un segmento della misura voluta o misurare la lunghezza di un segmento.
          </p>
        </div>
        <div class="flex justify-center">
          <%= image_tag "nvi5_mat/p112/p112_03.jpg", class: "max-w-full", alt: "Riga graduata" %>
        </div>
      </div>
    </div>

    <!-- 3. Le squadre -->
    <div>
      <div class="p-3 bg-yellow-light rounded-full mb-4 flex items-start gap-2">
        <span class="inline-flex items-center justify-center w-6 h-6 bg-<%= @pagina.base_color %>-500 text-white rounded-full font-bold shrink-0 -ml-3">3</span>
        <p class="text-gray-700">
          Le <strong class="text-<%= @pagina.base_color %>-600">squadre</strong> servono per tracciare due rette perpendicolari o due rette parallele, verificare se due rette sono perpendicolari o se sono parallele.
        </p>
      </div>
      <div class="flex justify-center">
        <%= image_tag "nvi5_mat/p112/p112_04.jpg", class: "max-w-full md:max-w-2xl", alt: "Le squadre" %>
      </div>
    </div>

    <%= render 'shared/exercise_controls' %>
  </div>
</div>
