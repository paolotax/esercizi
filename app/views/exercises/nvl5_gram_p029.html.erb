<div class="max-w-6xl mx-auto p-3 md:p-8 bg-gradient-to-br from-indigo-50 via-white to-pink-50 rounded-3xl shadow-xl" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Exercise 4: Rewrite sentences with antonyms -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-blue-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-3">
      <span class="font-bold">4</span> Riscrivi le frasi sostituendo le parole sottolineate con i loro contrari.
    </h2>

    <% sentences = [
      { original: "Ho un poster di animali feroci e coraggiosi.", answer: "Ho un poster di animali docili e paurosi." },
      { original: "La casa di campagna del nonno è enorme.", answer: "La casa di campagna del nonno è minuscola." },
      { original: "Abbiamo comprato un orologio antico.", answer: "Abbiamo comprato un orologio moderno." },
      { original: "Si tratta di un quadro di un pittore famoso.", answer: "Si tratta di un quadro di un pittore sconosciuto." }
    ] %>

    <div class="space-y-4 text-base md:text-lg">
      <% sentences.each do |item| %>
        <div class="bg-blue-50 border-2 border-blue-200 rounded-xl px-3 py-3">
          <p class="text-gray-700 mb-3"><%= item[:original] %></p>
          <input type="text"
                 class="w-full border-b-2 border-blue-500 bg-white rounded px-3 py-2 focus:outline-none focus:border-blue-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= item[:answer] %>"
                 data-correct-answer="<%= item[:answer] %>"
                 placeholder="Riscrivi la frase..."
                 maxlength="100">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 5: Replace "parlare" with synonyms -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-purple-200 shadow-2xl p-4 md:p-6" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold text-purple-700 mb-3">
      <span class="font-bold">5</span> In ogni frase sostituisci il verbo parlare con uno di significato simile.
    </h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">
      Scegli tra: <em class="text-purple-600">urlare • strillare • sbraitare • mormorare • bisbigliare • chiacchierare • intervenire • prendere la parola • raccontare • spifferare • svelare</em>
    </p>

    <% parlare_exercises = [
      { sentence: "Finitela di parlare a voce alta!", answer: "urlare" },
      { sentence: "Mentre l'insegnante spiegava, Monica parlava a voce bassa con Andrea.", answer: "bisbigliava" },
      { sentence: "Alla riunione il Sindaco ha parlato per salutare i genitori.", answer: "preso la parola" },
      { sentence: "Ci siamo incontrati e Luca mi ha parlato di un suo segreto.", answer: "svelato" }
    ] %>

    <div class="space-y-4 text-base md:text-lg">
      <% parlare_exercises.each do |item| %>
        <div class="bg-purple-50 border-2 border-purple-200 rounded-xl px-3 py-3">
          <p class="text-gray-700 mb-2"><%= item[:sentence] %></p>
          <div class="flex items-center gap-2">
            <span class="text-gray-600">Finitela di</span>
            <input type="text"
                   class="flex-1 border-b-2 border-purple-500 bg-transparent text-center focus:outline-none focus:border-purple-700"
                   data-fill-blanks-target="input"
                   data-answer="<%= item[:answer] %>"
                   data-correct-answer="<%= item[:answer] %>"
                   placeholder="..."
                   maxlength="20">
            <span class="text-gray-600">!</span>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 6: Writing exercise -->
  <div class="mb-6 bg-white rounded-2xl border-4 border-green-200 shadow-2xl p-4 md:p-6">
    <h2 class="text-lg md:text-xl font-bold text-green-700 mb-3">
      <span class="font-bold">6</span> Utilizza i seguenti aggettivi per scrivere almeno tre frasi sul tuo quaderno.
    </h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">
      Poi trasforma le frasi che hai scritto in frasi di significato opposto.
    </p>

    <div class="bg-green-50 border-2 border-green-200 rounded-xl px-3 py-4">
      <div class="flex flex-wrap gap-2 justify-center text-base md:text-lg">
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">scarso</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">fragile</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">tenue</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">leggero</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">difficile</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">fioco</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">flebile</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">gigantesco</span>
        <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full font-semibold">indebolito</span>
      </div>
    </div>

    <div class="mt-4 bg-yellow-50 border-2 border-yellow-300 rounded-xl px-3 py-3">
      <p class="text-sm md:text-base text-gray-700 italic">
        💡 Esempio: "Il rumore era tenue." → "Il rumore era forte."
      </p>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "141", pagina_path("nvl5_gram_p141"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise Controls -->
  <div class="mt-6">
    <%= render 'shared/exercise_controls', color: 'indigo', current_page: 'nvl5_gram_p029' %>
  </div>
</div>
