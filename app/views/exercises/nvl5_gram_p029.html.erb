<div class="max-w-6xl mx-auto p-3 md:p-8 bg-white" data-controller="exercise-checker">

  <%= render 'shared/page_header', pagina: @pagina %>

  <!-- Exercise 4: Rewrite sentences with antonyms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">4</span>
      Riscrivi le frasi sostituendo le parole sottolineate con i loro contrari.
    </h2>

    <% sentences = [
      { original: "Ho un poster di animali feroci e coraggiosi.", answer: "Ho un poster di animali docili e paurosi." },
      { original: "La casa di campagna del nonno è enorme.", answer: "La casa di campagna del nonno è minuscola." },
      { original: "Abbiamo comprato un orologio antico.", answer: "Abbiamo comprato un orologio moderno." },
      { original: "Si tratta di un quadro di un pittore famoso.", answer: "Si tratta di un quadro di un pittore sconosciuto." }
    ] %>

    <div class="space-y-4 text-base md:text-lg">
      <% sentences.each do |item| %>
        <div class="rounded-lg p-3">
          <p class="text-gray-700 mb-3"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= item[:original] %></p>
          <input type="text"
                 class="w-full border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white px-2 py-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                 data-fill-blanks-target="input"
                 data-answer="<%= item[:answer] %>"
                 data-correct-answer="<%= item[:answer] %>"
                 maxlength="100">
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 5: Replace "parlare" with synonyms -->
  <div class="bg-white p-4 md:p-6 pb-8" data-controller="fill-blanks">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">5</span>
      In ogni frase sostituisci il verbo parlare con uno di significato simile.
    </h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">
      Scegli tra: <em class="text-<%= @pagina.base_color %>-600">urlare • strillare • sbraitare • mormorare • bisbigliare • chiacchierare • intervenire • prendere la parola • raccontare • spifferare • svelare</em>
    </p>

    <% parlare_exercises = [
      { sentence: "Finitela di parlare a voce alta!", answer: "urlare" },
      { sentence: "Mentre l'insegnante spiegava, Monica parlava a voce bassa con Andrea.", answer: "bisbigliava" },
      { sentence: "Alla riunione il Sindaco ha parlato per salutare i genitori.", answer: "preso la parola" },
      { sentence: "Ci siamo incontrati e Luca mi ha parlato di un suo segreto.", answer: "svelato" }
    ] %>

    <div class="space-y-4 text-base md:text-lg">
      <% parlare_exercises.each do |item| %>
        <div class="rounded-lg p-3">
          <p class="text-gray-700 mb-2"><span class="text-<%= @pagina.base_color %>-500">•</span> <%= item[:sentence] %></p>
          <div class="flex items-center justify-between gap-4">
            <span class="text-gray-600">Sinonimo:</span>
            <input type="text"
                   class="w-40 border-b-2 border-dotted border-<%= @pagina.base_color %>-500 bg-white text-right px-2 focus:outline-none focus:border-<%= @pagina.base_color %>-700"
                   data-fill-blanks-target="input"
                   data-answer="<%= item[:answer] %>"
                   data-correct-answer="<%= item[:answer] %>"
                   maxlength="20">
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Exercise 6: Writing exercise -->
  <div class="bg-white p-4 md:p-6 pb-8">
    <h2 class="text-lg md:text-xl font-bold mb-4 flex items-center gap-2">
      <span class="flex-shrink-0 bg-<%= @pagina.base_color %>-500 text-white rounded-full w-8 h-8 md:w-10 md:h-10 inline-flex items-center justify-center">6</span>
      Utilizza i seguenti aggettivi per scrivere almeno tre frasi sul tuo quaderno.
    </h2>
    <p class="text-sm md:text-base text-gray-600 mb-4">
      Poi trasforma le frasi che hai scritto in frasi di significato opposto.
    </p>

    <div class="rounded-lg p-3">
      <div class="flex flex-wrap gap-2 text-base md:text-lg">
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">scarso</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">fragile</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">tenue</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">leggero</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">difficile</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">fioco</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">flebile</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">gigantesco</span>
        <span class="bg-<%= @pagina.base_color %>-100 text-<%= @pagina.base_color %>-700 px-3 py-1 rounded-full font-semibold">indebolito</span>
      </div>
    </div>

    <div class="mt-4 bg-gray-50 rounded-lg p-3">
      <p class="text-sm md:text-base text-gray-700 italic">
        <span class="text-<%= @pagina.base_color %>-500">•</span> Esempio: "Il rumore era tenue." → "Il rumore era forte."
      </p>
    </div>
  </div>

  <div class="text-right text-sm text-gray-500 mb-6">
    Esercizi p. <%= link_to "141", pagina_path("nvl5_gram_p141"), class: "text-gray-600 hover:text-gray-800 underline" %>
  </div>

  <!-- Exercise Controls -->
  <%= render 'shared/exercise_controls' %>
</div>
