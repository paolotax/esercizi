<%# locals: (question:, esercizio:) %>
<%= turbo_frame_tag dom_id(question) do %>
  <div class="p-4 bg-indigo-50 dark:bg-indigo-900/20 border-l-4 border-indigo-500">
    <%= form_with model: question, url: dashboard_esercizio_question_path(esercizio, question), method: :patch, class: "space-y-4" do |form| %>
      <div class="flex items-center justify-between">
        <h4 class="font-medium text-gray-900 dark:text-white">Modifica <%= question.questionable_type %></h4>
        <%= link_to edit_dashboard_esercizio_path(esercizio),
            class: "text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200",
            data: { turbo_frame: "_top" } do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        <% end %>
      </div>

      <% type = question.questionable_type.downcase %>
      <% data = question.questionable.data.with_indifferent_access %>

      <% case type %>
      <% when "addizione" %>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Primo addendo</label>
            <input type="text" name="data[addends][]" value="<%= data[:addends]&.first %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Secondo addendo</label>
            <input type="text" name="data[addends][]" value="<%= data[:addends]&.second %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
        </div>

      <% when "sottrazione" %>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Minuendo</label>
            <input type="text" name="data[minuend]" value="<%= data[:minuend] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Sottraendo</label>
            <input type="text" name="data[subtrahend]" value="<%= data[:subtrahend] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
        </div>

      <% when "moltiplicazione" %>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Moltiplicando</label>
            <input type="text" name="data[multiplicand]" value="<%= data[:multiplicand] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Moltiplicatore</label>
            <input type="text" name="data[multiplier]" value="<%= data[:multiplier] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
        </div>

      <% when "divisione" %>
        <div class="grid grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Dividendo</label>
            <input type="text" name="data[dividend]" value="<%= data[:dividend] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Divisore</label>
            <input type="text" name="data[divisor]" value="<%= data[:divisor] %>"
                   class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
          </div>
        </div>

      <% when "abaco" %>
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Valore</label>
          <input type="number" name="data[value]" value="<%= data[:value] %>"
                 class="w-full px-3 py-2 border-2 border-gray-200 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
        </div>
      <% end %>

      <div class="flex justify-end gap-2">
        <%= link_to "Annulla", edit_dashboard_esercizio_path(esercizio),
            class: "px-4 py-2 text-gray-600 dark:text-gray-300 bg-gray-100 dark:bg-gray-700 rounded-xl hover:bg-gray-200 dark:hover:bg-gray-600 transition font-medium",
            data: { turbo_frame: "_top" } %>
        <%= form.submit "Salva", class: "px-4 py-2 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700 transition cursor-pointer font-medium" %>
      </div>
    <% end %>
  </div>
<% end %>
